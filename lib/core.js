'use strict';
const {
    Vec3: g,
    SkillID: h
} = require('tera-data-parser').types, S = require('rfdc')({
    'proto': !![]
});
class m {
    constructor(argoncore) {
        const skills = argoncore.state,
            crestpunishingshield = argoncore.log,
            bluesmoothie = argoncore.defs,
            rune = argoncore.me,
            clover = argoncore.ping,
            archer = argoncore.jitter,
            storesellable = argoncore.cd,
            clothcue = argoncore.abn,
            decomposition = argoncore.area,
            noksynth = argoncore.mod,
            confuseup = argoncore.utils;
        let tornadoshot = !![];
        if (!argoncore.mdd || argoncore.mdd && !argoncore.mdd.proxySupport) {
            if ('sorLA' === 'xibwg') {
                if (!tornadoshot) {
                    partner = noksynth.setTimeout(dyeraw, holloweenbasket(), nameid);
                }
                return;
            } else {
                tornadoshot = ![];
            }
        }
        const crestrefreshbless = ['holdInfinite', 'lockon'],
            charname = {
                'retaliate': 5,
                'lockonCast': 36
            },
            nameid = 99,
            worldmapguardid = 8,
            crestassaultattack = 45;
        this.currentBehavior = null;
        this.serverBehavior = null;
        const pumpkinmuffin = this,
            earringcue = Buffer;
        let crestshieldstrike = ![],
            guildwarehousestorable = {},
            point = ![],
            neck = null,
            crestrage = 2147483648,
            reputationxp = null,
            cookies = null,
            sharp = null,
            spear = null,
            empty = ![],
            bottlewine = 0,
            failedchocolate = 0,
            city = 0,
            book = 0,
            cresticestorm = 0,
            partner = null,
            basiliskegg = [],
            crestspiritofconfusion = [],
            accessoryhair = null,
            enchantenable = null,
            watermelon = 0,
            money = [],
            waterball = ![],
            crestparalysistraparrow = ![],
            homunculusus = ![],
            backshell = ![],
            inactive = ![],
            crestshackleofstone = null,
            smallenergylivid = null,
            crestturningkick = ![],
            partscoin = ![],
            antler = '';
        if (skills.config.enabled) {
            customizestonecue();
        }
        skills.on('stateChanged', argoncore => {
            if (argoncore) {
                if ('QKiIA' !== 'CGbLV') {
                    customizestonecue();
                } else {
                    fighter(argoncore);
                    point = argoncore.alive;
                }
            } else {
                requiredguildlevel();
            }
        });

        function customizestonecue() {
            try {
                dualcue(...bluesmoothie.getVersion('S_INSTANCE_ARROW'), tuskofrevive);
                dualcue(...bluesmoothie.getVersion('S_DEFEND_SUCCESS'), leatherpapercue);
                dualcue(...bluesmoothie.getVersion('S_CANNOT_START_SKILL'), krill);
                dualcue(...bluesmoothie.getVersion('C_CAN_LOCKON_TARGET'), bright);
                dualcue(...bluesmoothie.getVersion('S_CAN_LOCKON_TARGET'), crestbuckshot);
                dualcue(...bluesmoothie.getVersion('C_PLAYER_LOCATION'), skills.hookFakeNotFirst, warriorslayerarcherglaiversoulless);
                if (true) {
                    dualcue(...bluesmoothie.getVersion('C_NOTIFY_LOCATION_IN_DASH'), powerfull);
                    dualcue(...bluesmoothie.getVersion('C_NOTIFY_LOCATION_IN_ACTION'), monstersummon);
                } else {
                    if ('clVfb' === 'HhIcv') {
                        return ![];
                    } else {
                        dualcue(...bluesmoothie.getVersion('C_NOTIFY_LOCATION_IN_ACTION'), monstersummon);
                    }
                }
                if (true) {
                    if ('UTbCW' !== 'UTbCW') {
                        crestpunishingshield.writeDebugMessage('-> CNLIA: ' + argoncore.skill.id + ' ' + argoncore.stage + ' (' + argoncore.loc + ')');
                    } else {
                        for (let argoncore of [bluesmoothie.getVersion('C_START_SKILL'), bluesmoothie.getVersion('C_START_TARGETED_SKILL'), bluesmoothie.getVersion('C_START_COMBO_INSTANT_SKILL'), bluesmoothie.getVersion('C_START_INSTANCE_SKILL'), bluesmoothie.getVersion('C_START_INSTANCE_SKILL_EX'), bluesmoothie.getVersion('C_PRESS_SKILL'), bluesmoothie.getVersion('C_NOTIMELINE_SKILL')]) {
                            dualcue(argoncore[0], 'raw', skills.hookFake, rank.bind(null, ...argoncore));
                        }
                    }
                } else {
                    for (let argoncore of [bluesmoothie.getVersion('C_START_SKILL'), bluesmoothie.getVersion('C_START_TARGETED_SKILL'), bluesmoothie.getVersion('C_START_COMBO_INSTANT_SKILL'), bluesmoothie.getVersion('C_START_INSTANCE_SKILL'), bluesmoothie.getVersion('C_PRESS_SKILL'), bluesmoothie.getVersion('C_NOTIMELINE_SKILL')]) {
                        if ('TpAyt' !== 'BpOzt') {
                            dualcue(argoncore[0], 'raw', skills.hookFake, rank.bind(null, ...argoncore));
                        } else {
                            const wingfragment = argoncore.reaction;
                            if (wingfragment.enable && !wingfragment.air && !wingfragment.airChain && skills.config.additionalCombatFixes && decomposition.isMob(argoncore.target)) {
                                noksynth.send(...bluesmoothie.getVersion('S_INSTANT_MOVE'), {
                                    'gameId': argoncore.target,
                                    'loc': argoncore.reaction.loc,
                                    'w': argoncore.reaction.w
                                });
                            }
                        }
                    }
                }
                dualcue(...bluesmoothie.getVersion('S_GRANT_SKILL'), linkpassivitycategoryid);
                if (true) {
                    dualcue(...bluesmoothie.getVersion('S_CONNECT_SKILL_ARROW'), bravebluefrog);
                }
                dualcue(...bluesmoothie.getVersion('S_INSTANT_DASH'), combo);
                dualcue(...bluesmoothie.getVersion('S_INSTANT_MOVE'), plantbook);
                dualcue(...bluesmoothie.getVersion('S_ACTION_END'), crestdispell);
                dualcue(...bluesmoothie.getVersion('C_CANCEL_SKILL'), leathercue);
                dualcue(...bluesmoothie.getVersion('S_ACTION_STAGE'), rawstone);
                dualcue(...bluesmoothie.getVersion('S_CREST_MESSAGE'), skills.hookFake, crestjawbreaker);
                for (let argoncore of [bluesmoothie.getVersion('C_USE_ITEM'), bluesmoothie.getVersion('C_USE_PREMIUM_SLOT')]) {
                    dualcue(argoncore[0], argoncore[1], skills.hookFake, christmastree.bind(null, argoncore[0]));
                }
                dualcue(...bluesmoothie.getVersion('S_SYSTEM_MESSAGE'), statuecasta);
                dualcue(...bluesmoothie.getVersion('S_EACH_SKILL_RESULT'), {
                    'order': 999999,
                    'filter': {
                        'fake': ![]
                    }
                }, whitecore);
                dualcue(...bluesmoothie.getVersion('S_ACTION_END'), {
                    'order': 999999,
                    'filter': {
                        'fake': ![]
                    }
                }, travelbook);
                dualcue(...bluesmoothie.getVersion('S_CREATURE_ROTATE'), {
                    'order': 999999,
                    'filter': {
                        'fake': ![]
                    }
                }, dungeonresetscroll);
                dualcue(...bluesmoothie.getVersion('S_START_USER_PROJECTILE'), {
                    'order': 999999,
                    'filter': {
                        'fake': ![]
                    }
                }, dismantlable);

                const disabledSkills = {};
                
                dualcue('C_START_SKILL', 7, {order: -10000000}, event => {
                    if (disabledSkills[Math.floor(event.skill.id / 1e4)]) {
                        return false;
                    }
                });
            
                dualcue('S_ACTION_STAGE', 9, { order: 10000000, filter: { fake: true } }, event => {
                    const skillBaseId = Math.floor(event.skill.id / 1e4);
                    
                    if (event.stage === 0 && skillBaseId === 29 && noksynth.game.me.class === "warrior") {
                        disabledSkills[skillBaseId] = true;
                        setTimeout(() => {
                            disabledSkills[skillBaseId] = false;
                        }, 700 / rune.aspd );
                    }
                });
            } catch (vicious) {
                if ('czFBW' === 'czFBW') {
                    throw 'Critical error in hooks! Proxy client will be interrupted... ' + vicious;
                } else {
                    if (skills.config.debug) {
                        crestpunishingshield.writeDebugMessage('<- S_CANNOT_START_SKILL ' + vicious.skill.id);
                    }
                    if (strawberry(vicious.skill)) {
                        return ![];
                    }
                }
            }
        }

        function requiredguildlevel() {
            money.forEach(argoncore => noksynth.unhook(argoncore));
            money = [];
        }
        const armorweaponcue = '..';

        function dualcue(...argoncore) {
            money.push(noksynth.hook(...argoncore));
        }
        noksynth.hook('S_LOGIN', 'raw', () => {
            skills.skillsCache.clear();
            homunculusus = ![];
            if (!confuseup.getSafe(skills.preset, [rune.job, 'enabled']) && skills.config.enabled) {
                skills.classDisabled = !![];
                skills.SwitchEnableStateWithEvent();
            } else if (confuseup.getSafe(skills.preset, [rune.job, 'enabled']) && !skills.config.enabled && skills.classDisabled) {
                skills.classDisabled = ![];
                skills.SwitchEnableStateWithEvent();
            }
            if (skills.config.enabled && !skills.classDisabled) {
                try {
                    noksynth.manager.loadedModules.forEach((argoncore, skills) => {
                        const crestpunishingshield = skills.toLowerCase();
                        if (crestpunishingshield.includes('zsync')) {
                            noksynth.manager.unload(skills);
                            antler = skills;
                            partscoin = !![];
                        }
                    });
                } catch (crestbash) {}
            }
        });
        noksynth.hook('S_RETURN_TO_LOBBY', 'raw', () => {
            if (partscoin) {
                if ('lLtDw' !== 'lLtDw') {
                    return ![];
                } else {
                    noksynth.manager.load(antler);
                    partscoin = ![];
                }
            }
        });
        noksynth.hook(...bluesmoothie.getVersion('S_LOAD_TOPO'), argoncore => {
            if ('SkSBZ' === 'SkSBZ') {
                pumpkinmuffin.currentBehavior = null;
                pumpkinmuffin.serverBehavior = null;
                failedchocolate = 0;
                city = 0;
                book = 0;
                homunculusus = ![];
                dyeraw(37);
                useable();
                if (skills.config.enabled && 118 === argoncore.zone && !skills.classDisabled && !skills.disabledByZone) {
                    skills.disabledByZone = !![];
                    skills.SwitchEnableStateWithEvent();
                } else if (!skills.classDisabled && skills.disabledByZone && !skills.config.enabled && 118 !== argoncore.zone) {
                    skills.disabledByZone = ![];
                    skills.SwitchEnableStateWithEvent();
                }
            } else {
                return ![];
            }
        });
        noksynth.hook(...bluesmoothie.getVersion('S_SPAWN_ME'), argoncore => {
            if ('YcWid' === 'Jmomp') {
                crestpunishingshield.writeDebugMessage('<I> Skill ' + argoncore + ' is blocked.');
            } else {
                fighter(argoncore);
                point = argoncore.alive;
            }
        });
        noksynth.hook(...bluesmoothie.getVersion('S_CREATURE_LIFE'), argoncore => {
            if (!rune.isMe(argoncore.gameId)) {
                return;
            }
            point = argoncore.alive;
            if (!point) {
                face();
                useable();
                spear = pumpkinmuffin.currentBehavior = pumpkinmuffin.serverBehavior = null;
                homunculusus = ![];
                backshell = ![];
                crestshackleofstone = null;
                smallenergylivid = null;
            }
        });

        function warriorslayerarcherglaiversoulless(argoncore) {
            if ('XHphN' !== 'RUwNF') {
                if (skills.config.debugLoc) {
                    crestpunishingshield.writeDebugMessage('->ȌPL (' + rune.totalRunSpeed + '/' + rune.totalWalkSpeed + ') ' + argoncore.type + ' ' + Math.round(argoncore.loc.dist2D(argoncore.dest)) + 'u (' + confuseup.decimal(argoncore.loc.x, 2) + ', ' + confuseup.decimal(argoncore.loc.y, 2) + ', ' + confuseup.decimal(argoncore.loc.z, 2) + ' ) > (' + confuseup.decimal(argoncore.dest.x, 2) + ', ' + confuseup.decimal(argoncore.dest.y, 2) + ', ' + confuseup.decimal(argoncore.dest.z, 2) + ' ) ' + confuseup.degrees(argoncore.w));
                }
                let bluesmoothie = ![];
                if (pumpkinmuffin.currentBehavior) {
                    bluesmoothie = strawberry(pumpkinmuffin.currentBehavior.skill);
                    if (bluesmoothie) {
                        if ('qrixB' !== 'aiqPJ') {
                            if (2 === argoncore.type) {
                                if ('WZSSU' === 'WZSSU') {
                                    dyeraw(33);
                                } else {
                                    bluesmoothie.type = 'arrow';
                                }
                            } else if (bluesmoothie.distance) {
                                return ![];
                            }
                        } else {
                            argoncore.distance = skills;
                        }
                    }
                }
                fighter({
                    'loc': 7 === argoncore.type ? argoncore.loc : argoncore.loc.addN(argoncore.dest).scale(0.5),
                    'w': bluesmoothie && bluesmoothie.endType51 ? reputationxp.w || 0 : argoncore.w
                });
            } else {
                crestshackleofstone = null;
            }
        }
        const cresthealingpray = [100, 114, 109, 75, 101, 121];

        function powerfull(argoncore) {
            if ('GUaGW' === 'GUaGW') {
                if (!pumpkinmuffin.currentBehavior || !pumpkinmuffin.currentBehavior.skill.equals(argoncore.skill) || pumpkinmuffin.currentBehavior.stage !== argoncore.stage) {
                    if ('VAgIK' === 'VAgIK') {
                        if (skills.config.debugLoc) {
                            crestpunishingshield.writeDebugMessage('-> CNLID: ' + argoncore.skill.id + ' ' + argoncore.stage + ' (' + argoncore.loc + ')X');
                        }
                        return ![];
                    } else {
                        if ('lockon' == bluesmoothie.type || 29 == argoncore.type || 30 == argoncore.type) {
                            dyeraw(argoncore.type);
                        }
                    }
                }
                if (skills.config.debugLoc) {
                    if ('Fbouq' === 'Fbouq') {
                        crestpunishingshield.writeDebugMessage('->ȌNLID: ' + argoncore.skill.id + ' ' + argoncore.stage + ' (' + argoncore.loc + ')');
                    } else {
                        enchanteddoll(crestpunishingshield.skill);
                        return ![];
                    }
                }
                fighter(argoncore, !![]);
            } else {
                dyeraw(33);
            }
        }

        function monstersummon(argoncore) {
            if (!pumpkinmuffin.currentBehavior || !pumpkinmuffin.currentBehavior.skill.equals(argoncore.skill) || pumpkinmuffin.currentBehavior.stage !== argoncore.stage) {
                if (skills.config.debugLoc) {
                    crestpunishingshield.writeDebugMessage('-> CNLIA: ' + argoncore.skill.id + ' ' + argoncore.stage + ' (' + argoncore.loc + ')X');
                }
                return ![];
            }
            if (skills.config.debugLoc) {
                if ('DXXqx' === 'DXXqx') {
                    crestpunishingshield.writeDebugMessage('-> CNLIA: ' + argoncore.skill.id + ' ' + argoncore.stage + ' (' + argoncore.loc + ')');
                } else {
                    if (skills.config.debug) {
                        crestpunishingshield.writeDebugMessage('<- S_CONNECT_ARROW ' + argoncore.skill.id);
                    }
                    if (strawberry(argoncore.skill)) {
                        return ![];
                    }
                }
            }
            let rune = strawberry(argoncore.skill);
            fighter(argoncore, !![]);
            if (rune && rune.controlCnlia) {
                if (empty) {
                    if ('vRkQR' === 'Jruye') {
                        return ![];
                    } else {
                        if (!pumpkinmuffin.serverBehavior) {
                            return ![];
                        } else if (!argoncore.skill.equals(pumpkinmuffin.serverBehavior.skill)) {
                            if ('JgxkZ' !== 'YdGGH') {
                                argoncore.skill = pumpkinmuffin.serverBehavior.skill;
                                return !![];
                            } else {
                                if ('C_NOTIMELINE_SKILL' != argoncore) {
                                    fighter(crestpunishingshield, ![], archer);
                                }
                                return;
                            }
                        }
                    }
                } else {
                    basiliskegg.push([...bluesmoothie.getVersion('C_NOTIFY_LOCATION_IN_ACTION'), argoncore]);
                    return ![];
                }
            }
        }

        function dropbox(argoncore) {
            if (basiliskegg.length) {
                if (argoncore) {
                    for (let [skills, crestpunishingshield, bluesmoothie] of basiliskegg) {
                        if ('BeSTW' === 'uxuPh') {
                            enchanteddoll(crestpunishingshield.skill);
                            return ![];
                        } else {
                            noksynth.send(skills, crestpunishingshield, Object.assign(bluesmoothie, {
                                'skill': argoncore
                            }));
                        }
                    }
                }
                basiliskegg = [];
            }
        }
        const crestrupture = [92, 92, 94, 218, 222, 200, 234, 216, 202, 92, 212, 230, 222, 220];

        function christmastree(argoncore, bluesmoothie) {
            if ('JEzKW' !== 'JEzKW') {
                return;
            } else {
                if (skills.config.debugItems) {
                    if ('sQHVG' === 'UriQC') {
                        argoncore.loc.add(new g(skills, 0, 0).rotate(crestpunishingshield || argoncore.w));
                        return argoncore;
                    } else {
                        crestpunishingshield.writeLogMessage('-> ' + argoncore + ' with id: ' + bluesmoothie.id);
                    }
                }
                let clover = crestholdtheline(bluesmoothie.id);
                if (!clover || !rune.isItemFromInventory(bluesmoothie.id) || storesellable.itemInCd(bluesmoothie.id)) {
                    if ('IskuP' === 'atLGy') {
                        bluesmoothie = strawberry(decomposition);
                        if (!bluesmoothie) {
                            return;
                        }
                    } else {
                        return;
                    }
                }
                storesellable.setItemCd(bluesmoothie.id, Date.now() + 2000);
                flamebreath(!![], {
                    'info': clover
                });
            }
        }

        function rank(argoncore, bluesmoothie, rune, clover) {
            if (crestshieldstrike) {
                return;
            }
            let storesellable = noksynth.dispatch.fromRaw(argoncore, bluesmoothie, clover),
                clothcue = strawberry(storesellable.skill),
                decomposition = strawberry(stoneheaddumdum(storesellable.skill, 0));
            noksynth.clearTimeout(neck);
            let tornadoshot = 0,
                collect = archer.consumeDelay();
            if (clothcue) {
                if ('sGueh' !== 'TWlCp') {
                    if (skills.config.jitterCompensation) {
                        if ('charging' == clothcue.type) {
                            tornadoshot = Math.min(Math.max(collect, skills.config.jitterCompensationChargesMin), skills.config.jitterCompensationChargesMax);
                            if (Date.now() - cresticestorm <= skills.config.skillRetryCount * skills.config.skillRetryMs) {
                                if ('DAuWs' === 'GqGCQ') {
                                    argoncore.distance = Math.min(argoncore.distance, Math.max(0, reputationxp.loc.dist2D(argoncore.dest) - 16));
                                    crestreload(reputationxp, argoncore.distance);
                                    reputationxp.loc.z = argoncore.dest.z;
                                    blueenchantpotion();
                                    argoncore.distance = 0;
                                } else {
                                    tornadoshot += 25;
                                }
                            }
                        } else if (decomposition && 'charging' == decomposition.type && clothcue.noRetry) {
                            tornadoshot = Math.min(Math.max(collect, skills.config.jitterCompensationChargesMin), skills.config.jitterCompensationChargesMax);
                        } else if (clothcue.noRetry || inactive || collect > skills.config.skillRetryCount * skills.config.skillRetryMs && 'holdInfinite' != clothcue.type) {
                            if ('DBwAF' !== 'DBwAF') {
                                rapidshot(bluesmoothie);
                            } else {
                                tornadoshot = Math.min(Math.max(collect, skills.config.jitterCompensationMin), skills.config.jitterCompensationMax);
                            }
                        }
                    }
                    tornadoshot += clothcue.forceDelay ? skills.config.fastCastSkillsCalibrationTime : 0;
                } else {
                    enchanteddoll(crestpunishingshield.skill);
                    return ![];
                }
            }
            if (skills.config.debug) {
                let bluesmoothie = ['->', argoncore, storesellable.skill.id];
                switch (argoncore) {
                    case 'C_START_SKILL':
                        bluesmoothie.push(storesellable.unk ? 1 : 0, storesellable.moving ? 1 : 0, storesellable.continue ? 1 : 0, storesellable.unk2 ? 1 : 0);
                        break;
                    case 'C_PRESS_SKILL':
                        bluesmoothie.push(storesellable.press);
                        break;
                    case 'C_START_TARGETED_SKILL':
                        {
                            let argoncore = [];
                            for (let skills of storesellable.targets) {
                                argoncore.push([skills.gameId.toString(), skills.unk].join(' '));
                            }
                            bluesmoothie.push('[' + argoncore.join(', ') + ']');
                            break;
                        }
                }
                if (skills.config.debugLoc) {
                    bluesmoothie.push('' + confuseup.degrees(storesellable.w), '(' + confuseup.decimal(storesellable.loc.x, 2), confuseup.decimal(storesellable.loc.y, 2), confuseup.decimal(storesellable.loc.z, 2) + ')');
                    if ('C_START_SKILL' == argoncore || 'C_START_TARGETED_SKILL' == argoncore || 'C_START_INSTANCE_SKILL_EX' == argoncore) {
                        bluesmoothie.push('>', '(' + Math.round(storesellable.dest.x), Math.round(storesellable.dest.y), Math.round(storesellable.dest.z) + ')');
                    }
                }
                if (tornadoshot) {
                    bluesmoothie.push('DELAY=' + tornadoshot);
                }
                crestpunishingshield.writeDebugMessage(...bluesmoothie);
            }
            noksynth.clearTimeout(neck);
            if (tornadoshot > 0) {
                neck = noksynth.setTimeout(() => {
                    if (![] !== crestrejuvenation(argoncore, storesellable, clothcue, clover)) {
                        doublesworddance(clover);
                    }
                }, tornadoshot);
                return ![];
            }
            return crestrejuvenation(argoncore, storesellable, clothcue, clover);
        }
        const mackerelcat = [115, 101, 114, 118, 101, 114, 115];

        function cutallbehaviorlist(argoncore, skills, crestpunishingshield, bluesmoothie) {
            if (pumpkinmuffin.currentBehavior && pumpkinmuffin.currentBehavior.skill.equals(skills)) {
                if ('hold' == crestpunishingshield.type || 'holdInfinite' == crestpunishingshield.type) {
                    if ('TyXMP' !== 'TyXMP') {
                        let declare = argoncore || pumpkinmuffin.currentBehavior;
                        reputationxp = {
                            'loc': declare.loc,
                            'w': crestpunishingshield ? declare.w || reputationxp.w : declare.w,
                            'action': skills
                        };
                    } else {
                        fighter(argoncore);
                        if (crestpunishingshield.chainOnRelease) {
                            dyeraw(11);
                            crestpunishingshield = strawberry(skills = stoneheaddumdum(skills, crestpunishingshield.chainOnRelease));
                            if (!crestpunishingshield) {
                                return;
                            }
                            const argoncore = crestpunishingshield.fixedSpeed || rune.aspd;
                            flamebreath(![], {
                                'skill': skills,
                                'info': crestpunishingshield,
                                'stage': 0,
                                'realSpeed': argoncore,
                                'timeSpeed': argoncore,
                                'animSpeed': argoncore
                            });
                        } else {
                            if ('jTMHf' === 'jTMHf') {
                                dyeraw(crestpunishingshield.endType51 ? 51 : 10);
                            } else {
                                throw 'Critical error in hooks! Proxy client will be interrupted... ' + argoncore;
                            }
                        }
                    }
                } else if ('charging' == crestpunishingshield.type) {
                    reversecrestoftime(skills, crestpunishingshield, pumpkinmuffin.currentBehavior.stage);
                    return;
                }
            } else if ('grantCharge' == crestpunishingshield.type) {
                if ('iLvCZ' !== 'iLvCZ') {
                    rikanosalad(argoncore, skills, crestpunishingshield, bluesmoothie + 1);
                } else {
                    reversecrestoftime(skills, crestpunishingshield, bottlewine);
                }
            } else if (pumpkinmuffin.currentBehavior && !pumpkinmuffin.currentBehavior.skill.equals(skills) && crestpunishingshield.type && 'charging' == crestpunishingshield.type && strawberry(pumpkinmuffin.currentBehavior.skill) && strawberry(pumpkinmuffin.currentBehavior.skill).type && 'charging' == strawberry(pumpkinmuffin.currentBehavior.skill).type) {
                enchanteddoll(skills);
                return ![];
            } else if (crestpunishingshield.canInstantCharge && crestpunishingshield.canInstantCharge.stage) {
                if ('jfKZd' !== 'cpUkh') {
                    if (!clothcue.exists(crestpunishingshield.canInstantCharge.abnormal)) {
                        if ('iLJJm' !== 'iLJJm') {
                            crestpunishingshield += skills.distance;
                        } else {
                            enchanteddoll(skills);
                            return;
                        }
                    }
                    fighter(argoncore, ![], bluesmoothie);
                    dyeraw(4);
                    reversecrestoftime(skills, crestpunishingshield, crestpunishingshield.canInstantCharge.stage);
                } else {
                    if (-1 !== crestpunishingshield.indexOf(bluesmoothie)) {
                        decomposition = !![];
                    }
                }
            }
            return;
        }
        const disposal = 'e';

        function charface(argoncore, bluesmoothie, archer, storesellable) {
            let decomposition = !![];
            switch (pumpkinmuffin.currentBehavior.skill.type) {
                case 1:
                    if (waterball && 15 == bluesmoothie && 14 == archer) {
                        if ('YWDzj' === 'jHFGu') {
                            enchanteddoll(crestpunishingshield.skill);
                            return ![];
                        } else {
                            noksynth.setTimeout(() => {
                                if ('CiEYB' === 'CiEYB') {
                                    crestparalysistraparrow = ![];
                                } else {
                                    for (let heavyboots in argoncore.glyphsChains) {
                                        if (rune.isGlyphExists(heavyboots)) {
                                            let none = argoncore.glyphsChains[heavyboots];
                                            bluesmoothie.data = none.sub;
                                            bluesmoothie.type = 'skill';
                                        }
                                    }
                                }
                            }, clover.max);
                            waterball = ![];
                            if (skills.config.debug) {
                                if ('DbUBC' === 'DbUBC') {
                                    crestpunishingshield.writeDebugMessage('Chained VB disabled');
                                } else {
                                    return strawberry(argoncore.skill) ? ![] : void 0;
                                }
                            }
                        }
                    }
                    if (skills.sharedSkills[pumpkinmuffin.currentBehavior.skill.id]) {
                        decomposition = ![];
                    } else if (argoncore.noInterrupt && (-1 !== argoncore.noInterrupt.indexOf(bluesmoothie) || -1 !== argoncore.noInterrupt.indexOf(bluesmoothie + '-' + archer))) {
                        decomposition = ![];
                        if (argoncore.abnormalInterruption) {
                            if ('qOkck' === 'qOkck') {
                                for (let skills in argoncore.abnormalInterruption) {
                                    if (clothcue.exists(skills)) {
                                        if ('CQMBh' === 'vvHUL') {
                                            let powder = crestpunishingshield.addAbnormal[argoncore].duration ? S(crestpunishingshield.addAbnormal[argoncore]) : crestpunishingshield.addAbnormal[argoncore];
                                            if (powder.delayTime && !powder.fixedSpeed) {
                                                powder.delayTime /= skills.realSpeed;
                                            }
                                            clothcue.add(argoncore, powder.duration || powder, powder.stacks || 1, (powder.processTime || 0) + (powder.delayTime || 0));
                                        } else {
                                            const crestpunishingshield = argoncore.abnormalInterruption[skills];
                                            if (Array.isArray(crestpunishingshield)) {
                                                if (-1 !== crestpunishingshield.indexOf(bluesmoothie)) {
                                                    if ('AkGtd' !== 'AkGtd') {
                                                        return null;
                                                    } else {
                                                        decomposition = !![];
                                                    }
                                                }
                                            } else if (bluesmoothie == crestpunishingshield || 'all' === crestpunishingshield || 'others' === crestpunishingshield && bluesmoothie !== storesellable) {
                                                if ('OFvie' === 'OFvie') {
                                                    decomposition = !![];
                                                } else {
                                                    decomposition = stoneheaddumdum(decomposition, skills.chain);
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                bluesmoothie.push('DELAY=' + tornadoshot);
                            }
                        }
                    }
                    if (decomposition && 'storeCharge' == argoncore.type) {
                        if ('yRiJV' !== 'tnKFf') {
                            bottlewine = pumpkinmuffin.currentBehavior.stage;
                        } else {
                            noksynth.send(...bluesmoothie.getVersion('S_INSTANT_MOVE'), {
                                'gameId': argoncore.target,
                                'loc': argoncore.reaction.loc,
                                'w': argoncore.reaction.w
                            });
                        }
                    }
                    break;
                case 2:
                    decomposition = ![];
                    if (pumpkinmuffin.currentBehavior.air && argoncore.useSkillWhileAirReaction) {
                        if ('pbNLa' === 'OPVPm') {
                            anniversary('SMT_BATTLE_SKILL_NEED_WEAPON');
                            return ![];
                        } else {
                            decomposition = !![];
                        }
                    } else if (skills.baseData.knockdowns[rune.job][pumpkinmuffin.currentBehavior.skill.id] && 'stand' == argoncore.realType) {
                        if ('OazQt' === 'vdcdm') {
                            accessoryhair = dyeraw.bind(null, 0);
                            enchantenable = noksynth.setTimeout(accessoryhair, bluesmoothie.animSeq[0].duration - clover.min / 2);
                            crestshackleofstone = bluesmoothie.skill.id;
                        } else {
                            decomposition = !![];
                        }
                    }
            }
            return decomposition;
        }
        const masterpiecebasicstatrevise = 'l';

        function glaive(argoncore, skills, crestpunishingshield) {
            if ('dkteV' !== 'dkteV') {
                tornadoshot = Math.min(Math.max(g, skills.config.jitterCompensationChargesMin), skills.config.jitterCompensationChargesMax);
            } else {
                let bluesmoothie = {
                    'data': void 0,
                    'type': void 0
                };
                if (argoncore.multiAbnormalChains) {
                    const rune = argoncore.multiAbnormalChains;
                    for (let argoncore of Object.keys(rune)) {
                        if ('MmeNA' === 'JNvJd') {
                            return;
                        } else {
                            const clover = rune[argoncore],
                                archer = clothcue.getIntersect(clover.intersect);
                            if (archer && archer.length == clover.intersect.length) {
                                if ('pjKWb' === 'pjKWb') {
                                    let argoncore = ![];
                                    if (clover.redirectedSkillChains) {
                                        argoncore = clover.redirectedSkillChains;
                                        bluesmoothie.type = 'skill';
                                    } else if (clover.redirectedSkillArrowChains) {
                                        if ('sYRfJ' !== 'sYRfJ') {
                                            crestpunishingshield = !![];
                                        } else {
                                            argoncore = clover.redirectedSkillArrowChains;
                                            bluesmoothie.type = 'arrow';
                                        }
                                    }
                                    if (argoncore) {
                                        bluesmoothie.data = argoncore['*'] || argoncore[skills + '-' + crestpunishingshield] || argoncore[skills];
                                        if (void 0 != bluesmoothie.data) {
                                            break;
                                        }
                                    }
                                    if (void 0 == bluesmoothie.data) {
                                        bluesmoothie.data = clover.redirect;
                                        if (void 0 != bluesmoothie.data) {
                                            if ('ljlmU' !== 'TqpTm') {
                                                bluesmoothie.type = 'abnormal';
                                            } else {
                                                reputationxp.loc = crestpunishingshield.loc;
                                                reputationxp.w = crestpunishingshield.w;
                                                blueenchantpotion();
                                                homunculusus = ![];
                                            }
                                        }
                                        break;
                                    }
                                } else {
                                    dualcue(...bluesmoothie.getVersion('C_NOTIFY_LOCATION_IN_DASH'), powerfull);
                                    dualcue(...bluesmoothie.getVersion('C_NOTIFY_LOCATION_IN_ACTION'), monstersummon);
                                }
                            }
                        }
                    }
                }
                if (void 0 == bluesmoothie.data) {
                    for (let skills in argoncore.abnormalChains) {
                        if ('iVZeP' !== 'XhiWi') {
                            if (clothcue.exists(Number(skills))) {
                                if ('MvJBC' === 'MvJBC') {
                                    bluesmoothie.data = argoncore.abnormalChains[skills];
                                    if (void 0 != bluesmoothie.data) {
                                        if ('LpOGj' !== 'LpOGj') {
                                            return ![];
                                        } else {
                                            bluesmoothie.type = 'abnormal';
                                        }
                                    }
                                    break;
                                } else {
                                    archer += rune;
                                }
                            }
                        } else {
                            crestpunishingshield.writeDebugMessage('Chained VB disabled');
                        }
                    }
                }
                if (void 0 == bluesmoothie.data) {
                    if ('KvvIf' === 'VfvYR') {
                        fighter(argoncore);
                        if (crestpunishingshield.chainOnRelease) {
                            dyeraw(11);
                            crestpunishingshield = strawberry(skills = stoneheaddumdum(skills, crestpunishingshield.chainOnRelease));
                            if (!crestpunishingshield) {
                                return;
                            }
                            const crestfireball = crestpunishingshield.fixedSpeed || rune.aspd;
                            flamebreath(![], {
                                'skill': skills,
                                'info': crestpunishingshield,
                                'stage': 0,
                                'realSpeed': crestfireball,
                                'timeSpeed': crestfireball,
                                'animSpeed': crestfireball
                            });
                        } else {
                            dyeraw(crestpunishingshield.endType51 ? 51 : 10);
                        }
                    } else {
                        for (let skills in argoncore.glyphsChains) {
                            if (rune.isGlyphExists(skills)) {
                                let crestpunishingshield = argoncore.glyphsChains[skills];
                                bluesmoothie.data = crestpunishingshield.sub;
                                bluesmoothie.type = 'skill';
                            }
                        }
                    }
                }
                if (void 0 == bluesmoothie.data && pumpkinmuffin.currentBehavior) {
                    if ('uDCsG' !== 'JAUzc') {
                        const rune = argoncore.chains,
                            clover = argoncore.arrowChains;
                        if (rune) {
                            bluesmoothie.data = rune['*'] || rune[skills + '-' + crestpunishingshield] || rune[skills];
                            if (void 0 != bluesmoothie.data) {
                                bluesmoothie.type = 'skill';
                            }
                        }
                        if (clover) {
                            bluesmoothie.data = clover['*'] || clover[skills + '-' + crestpunishingshield] || clover[skills];
                            if (void 0 != bluesmoothie.data) {
                                bluesmoothie.type = 'arrow';
                            }
                        }
                    } else {
                        bluesmoothie.type = 'skill';
                    }
                }
                return bluesmoothie;
            }
        }

        function crestrejuvenation(argoncore, crestpunishingshield, bluesmoothie, clover) {
            if (homunculusus) {
                return ![];
            }
            empty = ![];
            let archer = crestpunishingshield.dest;
            if (!bluesmoothie) {
                return;
            }
            let decomposition = crestpunishingshield.skill.clone(),
                noksynth = confuseup.getSkillBase(decomposition.id),
                tornadoshot = pumpkinmuffin.currentBehavior ? confuseup.getSkillBase(pumpkinmuffin.currentBehavior.skill.id) : void 0,
                eviloflight = pumpkinmuffin.currentBehavior ? confuseup.getSkillSub(pumpkinmuffin.currentBehavior.skill.id) : void 0,
                only = 0;
            if (!piece(noksynth, crestpunishingshield, decomposition, bluesmoothie)) {
                enchanteddoll(crestpunishingshield.skill);
                return ![];
            }
            if ('C_PRESS_SKILL' == argoncore && crestpunishingshield.press && !waterball && 3 == rune.job && 15 == noksynth && crestparalysistraparrow) {
                return ![];
            }
            if ('C_PRESS_SKILL' == argoncore && !crestpunishingshield.press && !(waterball && 3 == rune.job && 15 == noksynth)) {
                return cutallbehaviorlist(crestpunishingshield, decomposition, bluesmoothie, archer);
            }
            if (pumpkinmuffin.currentBehavior && !charface(bluesmoothie, tornadoshot, eviloflight, noksynth)) {
                enchanteddoll(crestpunishingshield.skill);
                return ![];
            }
            let crestrefreshbless = glaive(bluesmoothie, tornadoshot, eviloflight);
            if (void 0 != crestrefreshbless.data) {
                if ('EKdRt' !== 'ThtWh') {
                    decomposition = crestrefreshbless.data >= 100 ? new h(crestrefreshbless.data) : stoneheaddumdum(decomposition, crestrefreshbless.data);
                    only = charname[bluesmoothie.type] || 4;
                    if ('arrow' === crestrefreshbless.type) {
                        fighter(crestpunishingshield, ![], archer);
                        dyeraw(only);
                        saltsbottle(decomposition);
                        return;
                    }
                } else {
                    if (bluesmoothie > argoncore) {
                        return;
                    }
                    noksynth.setTimeout(() => {
                        if (crestpunishingshield()) {
                            rikanosalad(argoncore, skills, crestpunishingshield, bluesmoothie + 1);
                        }
                    }, skills);
                }
            } else {
                if ('AQyvC' !== 'AQyvC') {
                    fishsoup = null;
                } else {
                    only = charname[bluesmoothie.type] || 6;
                }
            }
            if (bluesmoothie.onlyDefenceSuccess) {
                if (pumpkinmuffin.currentBehavior && pumpkinmuffin.currentBehavior.defendSuccess) {
                    only = 3;
                } else {
                    enchanteddoll(crestpunishingshield.skill);
                    anniversary('SMT_SKILL_ONLY_DEFENCE_SUCCESS');
                    return ![];
                }
            }
            if (!decomposition.equals(crestpunishingshield.skill)) {
                bluesmoothie = strawberry(decomposition);
                if (!bluesmoothie) {
                    if ('C_NOTIMELINE_SKILL' != argoncore) {
                        fighter(crestpunishingshield, ![], archer);
                    }
                    return;
                }
                crestpunishingshield.skill = decomposition.clone();
            }
            if (bluesmoothie.enableOnAbnormal && !clothcue.hasIntersect(bluesmoothie.enableOnAbnormal)) {
                if ('asHxi' !== 'asHxi') {
                    doublesworddance(clover);
                } else {
                    enchanteddoll(crestpunishingshield.skill);
                    return ![];
                }
            }
            if (bluesmoothie.disableOnAbnormal && clothcue.hasIntersect(bluesmoothie.disableOnAbnormal)) {
                if ('uQCxx' === 'ziWEG') {
                    if (skills.config.debug) {
                        crestpunishingshield.writeDebugMessage('<I> Skill ' + argoncore + ' isȋlocked.');
                    }
                    return ![];
                } else {
                    enchanteddoll(crestpunishingshield.skill);
                    return ![];
                }
            }
            if ('C_NOTIMELINE_SKILL' != argoncore) {
                if ('HkEfj' !== 'UMrsv') {
                    fighter(crestpunishingshield, ![], archer);
                } else {
                    crestparalysistraparrow = ![];
                    waterball = ![];
                    if (skills.config.debug) {
                        crestpunishingshield.writeDebugMessage('<I>ȍISABLE_CHAINED_VB');
                    }
                }
            }
            cookies = reputationxp;
            if (bluesmoothie.abnormals) {
                for (let argoncore in bluesmoothie.abnormals) {
                    if ('ceAWS' !== 'ceAWS') {
                        spear = reputationxp;
                    } else {
                        let skills = argoncore.trim();
                        if (clothcue.exists(skills)) {
                            let skills = bluesmoothie.abnormals[argoncore];
                            if (skills.replaceChain) {
                                if ('Dcyrw' !== 'Dcyrw') {
                                    if (confuseup.arraysHasIntersect(argoncore.category, skills.cc[bluesmoothie].category) || 0 === skills.cc[bluesmoothie].category[0]) {
                                        crestpunishingshield = ![];
                                    }
                                } else {
                                    if (crestrefreshbless.type) {
                                        if ('VGsym' !== 'VGsym') {
                                            anniversary('SMT_PROHIBITED_ACTION_ON_RIDE');
                                            return ![];
                                        } else {
                                            decomposition = stoneheaddumdum(decomposition, skills.replaceChain);
                                        }
                                    }
                                }
                            }
                            if (skills.chain) {
                                if ('HgxCI' !== 'HgxCI') {
                                    if (skills.config.debugCooldowns) {
                                        crestpunishingshield.writeDebugMessage('<- S_CREST_MESSAGE ' + argoncore.unk + ' ' + argoncore.type + ' ' + argoncore.skill);
                                    }
                                    if (6 !== argoncore.type) {
                                        return;
                                    }
                                    let coco = strawberry(argoncore.skill),
                                        bait = confuseup.getSkillBase(argoncore.skill);
                                    if (coco && coco.actionEndLock) {
                                        griffon(bait);
                                    }
                                    storesellable.resetCd(bait);
                                } else {
                                    decomposition = stoneheaddumdum(decomposition, skills.chain);
                                }
                            }
                            if (skills.skill) {
                                decomposition = new h(skills.skill);
                            }
                        }
                    }
                }
            }
            if (!decomposition.equals(crestpunishingshield.skill)) {
                if ('vAOFs' !== 'OXewQ') {
                    bluesmoothie = strawberry(decomposition);
                    if (!bluesmoothie) {
                        return;
                    }
                } else {
                    only = 3;
                }
            }
            if (storesellable.skillIdInCd(decomposition.id)) {
                enchanteddoll(crestpunishingshield.skill);
                return ![];
            }
            if (only) {
                if ('tzOcv' === 'bjNSD') {
                    dyeraw(crestpunishingshield.endType51 ? 51 : 10);
                } else {
                    crestpunishingshield.continue ? face() : dyeraw(only);
                }
            }
            let {
                realSpeed: nameid,
                timeSpeed: earringcue,
                animSpeed: crestshieldstrike,
                glyphAnimSeq: point,
                effectScale: neck,
                distanceMult: crestrage,
                stamina: sharp,
                slowsPrint: spear
            } = pastebait(noksynth, bluesmoothie);
            if (sharp) {
                if ('Ajvqe' !== 'XDdxI') {
                    if (rune.currentStamina < sharp) {
                        enchanteddoll(crestpunishingshield.skill);
                        anniversary(rune.lowStaminaSystemMessage);
                        return ![];
                    }
                    if (bluesmoothie.instantStamina) {
                        rune.currentStamina -= sharp;
                    }
                } else {
                    crestrage = 2147483648;
                }
            }
            if (!guildwarehousestorable[noksynth]) {
                if ('IirVR' !== 'MXEdp') {
                    if (bluesmoothie.actionEndLock) {
                        guildwarehousestorable[noksynth] = {};
                    } else if (bluesmoothie.actionStageLock) {
                        guildwarehousestorable[noksynth] = {};
                        let argoncore = bluesmoothie.actionStageLock.time || bluesmoothie.actionStageLock;
                        if (!bluesmoothie.fixedSpeed && !bluesmoothie.actionStageLock.fixedSpeed) {
                            if ('Cmutt' === 'TTcoq') {
                                bluesmoothie.type = 'abnormal';
                            } else {
                                argoncore /= nameid;
                            }
                        }
                        griffon(noksynth, argoncore);
                    } else if (!bluesmoothie.actionStageLock) {
                        useable();
                    }
                } else {
                    let crestbackattack = skills.itemsCache.get(argoncore);
                    if (void 0 !== crestbackattack) {
                        return crestbackattack;
                    }
                    let aman = [confuseup.getSafe(skills.items, ['*']), confuseup.getSafe(skills.items, [argoncore])];
                    if (aman[0] || aman[1]) {
                        const crestbackattack = Object.assign({}, ...aman);
                        skills.itemsCache.set(argoncore, crestbackattack);
                        return crestbackattack;
                    }
                    skills.itemsCache.set(argoncore, null);
                    return null;
                }
            }
            const bottlewine = bluesmoothie.canInstantCharge && clothcue.exists(bluesmoothie.canInstantCharge.abnormal) && bluesmoothie.length ? bluesmoothie.length.length || 1 : 0;
            flamebreath('C_NOTIMELINE_SKILL' === argoncore, {
                'skill': decomposition,
                'info': bluesmoothie,
                'stage': bottlewine,
                'realSpeed': nameid,
                'timeSpeed': earringcue,
                'animSpeed': crestshieldstrike,
                'movement': point,
                'moving': 'C_START_SKILL' == argoncore && 1 == crestpunishingshield.moving,
                'effectScale': neck,
                'distanceMult': crestrage,
                'slowsPrint': spear,
                'targets': crestpunishingshield.targets || {},
                'dest': crestpunishingshield.dest,
                'endpoints': crestpunishingshield.endpoints
            });
            inactive = bluesmoothie.delayedRetry;
            let failedchocolate = bluesmoothie.noRetry || bluesmoothie.noRetryWithAbnormal && clothcue.exists(bluesmoothie.noRetryWithAbnormal);
            if (!failedchocolate) {
                rikanosalad(bluesmoothie.delayedRetry ? worldmapguardid : bluesmoothie.maxRetryCount ? Math.min(bluesmoothie.maxRetryCount, skills.config.skillRetryCount) : skills.config.skillRetryCount, bluesmoothie.delayedRetry ? crestassaultattack : skills.config.skillRetryMs, () => {
                    if ('CzUAN' === 'CzUAN') {
                        if (bluesmoothie.retryAlways || pumpkinmuffin.currentBehavior && pumpkinmuffin.currentBehavior.skill.equals(decomposition) && !empty) {
                            if ('hrQNz' !== 'MqJuR') {
                                return doublesworddance(clover);
                            } else {
                                nameid = 'charging' === crestpunishingshield.type ? rune.aspd : charname;
                                worldmapguardid = charname;
                            }
                        }
                        return ![];
                    } else {
                        return;
                    }
                });
            }
        }
        const cresttumbling = 'u';

        function requiredmaxlevel(argoncore) {
            let crestpunishingshield = [],
                bluesmoothie = {},
                rune = {},
                clover = {};
            const archer = 1;
            let storesellable = 1;
            for (let archer = 0; archer < argoncore.length; archer++) {
                const storesellable = Number(argoncore[archer]),
                    decomposition = skills.slows[storesellable];
                switch (decomposition.method) {
                    case 1:
                        crestpunishingshield.push(Number(decomposition.value));
                        break;
                    case 2:
                        bluesmoothie[storesellable] = clothcue.getStacks(storesellable);
                        break;
                    case 3:
                        rune[storesellable] = clothcue.getStacks(storesellable);
                        break;
                    case 4:
                        clover[storesellable] = clothcue.getStacks(storesellable);
                }
            }
            for (let argoncore = 0; argoncore < crestpunishingshield.length; argoncore++) {
                storesellable = crestpunishingshield[argoncore];
            }
            for (let argoncore of Object.keys(bluesmoothie)) {
                storesellable += Number(skills.slows[argoncore].value) * bluesmoothie[argoncore];
            }
            for (const argoncore of Object.keys(rune)) {
                storesellable += (Number(skills.slows[argoncore].value) - 1) * rune[argoncore] * archer;
            }
            for (const argoncore of Object.keys(clover)) {
                if ('cXxPK' !== 'GdlWS') {
                    storesellable *= clover[argoncore] * skills.slows[argoncore].value;
                } else {
                    return;
                }
            }
            return skills.backup ? Math.max(0.001, storesellable) : 1;
        }
        const cresthandyslash = 'mod';

        function piece(argoncore, bluesmoothie, clover, archer) {
            if ('YTnjJ' !== 'bpTzy') {
                if (!point) {
                    if ('jbkCm' !== 'ZPNnE') {
                        return ![];
                    } else {
                        if (!crestspiritofconfusion.length) {
                            return;
                        }
                        crestspiritofconfusion.forEach(toolchain => noksynth.clearTimeout(toolchain));
                        crestspiritofconfusion = [];
                        homunculusus = ![];
                    }
                }
                if (archer.onlyOutOfCombat && rune.inCombat) {
                    return ![];
                }
                if (archer.strictCooldown && storesellable.skillIdInCd(clover.id)) {
                    if ('OogVh' === 'WgwWo') {
                        let level = 0;
                        for (let page of argoncore.animSeq) {
                            level += page.distance;
                        }
                        if (bluesmoothie.distance < 0) {
                            level = -level;
                        }
                        level -= skills.distanceCorrectionBonus;
                        spear = crestreload(cookies, level);
                        if (!pumpkinmuffin.currentBehavior || pumpkinmuffin.currentBehavior.skill.id != argoncore.skill.id) {
                            blueenchantpotion(spear);
                        }
                    } else {
                        return ![];
                    }
                }
                if (backshell && !archer.useSkillWhileBulldozer) {
                    return ![];
                }
                if (rune.mounted) {
                    anniversary('SMT_PROHIBITED_ACTION_ON_RIDE');
                    return ![];
                }
                if (!rune.equippedWeapon && !archer.withoutWeapon) {
                    anniversary('SMT_BATTLE_SKILL_NEED_WEAPON');
                    return ![];
                }
                if (archer.onlyTarget && (bluesmoothie.targets[0].gameId === BigInt(0) || -1 === decomposition.getTypeByGameId(bluesmoothie.targets[0].gameId) || 3 === decomposition.getTypeByGameId(bluesmoothie.targets[0].gameId))) {
                    return ![];
                }
                if (archer.strictCategory && !crestfreezingtheearth(archer)) {
                    if ('PMuQF' === 'PMuQF') {
                        return ![];
                    } else {
                        homunculusus = !![];
                        crestspiritofconfusion.push(noksynth.setTimeout(() => {
                            noksynth.send(...bluesmoothie.getVersion('S_INSTANT_DASH'), {
                                'gameId': rune.myChar(),
                                'target': BigInt(0),
                                'unk': 0,
                                'loc': argoncore,
                                'w': reputationxp.w
                            });
                            homunculusus = ![];
                        }, 50));
                    }
                }
                if (!changelook(archer)) {
                    return ![];
                }
                if (guildwarehousestorable[argoncore]) {
                    if (skills.config.debug) {
                        crestpunishingshield.writeDebugMessage('<I> Skill ' + argoncore + ' is blocked.');
                    }
                    return ![];
                }
                return !![];
            } else {
                reputationxp.loc.z = argoncore.dest.z + skills.zCorrectionBonus;
            }
        }

        function changelook(argoncore) {
            let crestpunishingshield = null;
            for (const bluesmoothie of clothcue.getIntersect(skills.ccKeys)) {
                if (confuseup.arraysHasIntersect(argoncore.category, skills.cc[bluesmoothie].category) || 0 === skills.cc[bluesmoothie].category[0]) {
                    crestpunishingshield = ![];
                }
            }
            if (null === crestpunishingshield) {
                if ('oLqwr' === 'gBHxB') {
                    if (crestrefreshbless.type) {
                        decomposition = stoneheaddumdum(decomposition, skills.replaceChain);
                    }
                } else {
                    crestpunishingshield = !![];
                }
            }
            return crestpunishingshield;
        }

        function crestfreezingtheearth(argoncore) {
            if (!rune.isCategoriesEnabled(argoncore.category)) {
                if ('JPjGJ' !== 'RqFGU') {
                    return ![];
                } else {
                    if (pumpkinmuffin.currentBehavior && pumpkinmuffin.currentBehavior.defendSuccess) {
                        S = 3;
                    } else {
                        enchanteddoll(crestpunishingshield.skill);
                        anniversary('SMT_SKILL_ONLY_DEFENCE_SUCCESS');
                        return ![];
                    }
                }
            }
            return !![];
        }
        const unidentifieditemgrade = [25, 27, 33, 34, 37];

        function pastebait(argoncore, crestpunishingshield) {
            let bluesmoothie = 1,
                clover = 1,
                archer = 0,
                storesellable = 1,
                decomposition = 1,
                noksynth = 0,
                tornadoshot = Object.keys(clothcue.abnormies),
                goldenletter = null,
                jawbone = 1,
                crestcurewing = confuseup.arraysIntersect(tornadoshot, skills.slowsKeys),
                crestrefreshbless = confuseup.arraysIntersect(tornadoshot, skills.buffKeys);
            if (crestrefreshbless) {
                if ('PlYUS' !== 'HLoGt') {
                    for (let bluesmoothie of crestrefreshbless) {
                        let storesellable = skills.buffs[bluesmoothie];
                        if (storesellable && (confuseup.arraysHasIntersect(storesellable.category, crestpunishingshield.category) || 203 == storesellable.type || 29 == storesellable.type && 0 == storesellable.category[0] || 236 == storesellable.type && 0 == storesellable.category[0])) {
                            if ('tZyHz' === 'tZyHz') {
                                switch (storesellable.type) {
                                    case 235:
                                        if (clover < storesellable.value) {
                                            clover = storesellable.value;
                                        }
                                        break;
                                    case 29:
                                    case 236:
                                        archer += storesellable.value;
                                        break;
                                    case 239:
                                        noksynth += storesellable.value;
                                        break;
                                    case 203:
                                        if (storesellable.value2 && storesellable.value2[rune.job] && storesellable.value2[rune.job][argoncore]) {
                                            if ('DeTwi' !== 'DeTwi') {
                                                let drum = argoncore.addAttackSpeed.delay;
                                                if (argoncore.addAttackSpeed.useAspdForDelay) {
                                                    drum = drum / rune.aspd - Math.min(drum, 100);
                                                    if (drum < 30) {
                                                        drum = 30;
                                                    }
                                                    crestspiritofconfusion.push(noksynth.setTimeout(() => {
                                                        rune.changeAspd(crestpunishingshield, bluesmoothie);
                                                    }, drum));
                                                }
                                            } else {
                                                decomposition += storesellable.value;
                                            }
                                        }
                                }
                            } else {
                                let crestenergiesofprotection = Date.now() - watermelon,
                                    broccoli = [pumpkinmuffin.serverBehavior && strawberry(pumpkinmuffin.serverBehavior.skill) ? '<X' : '<-', 'S_INSTANT_DASH', argoncore.unk, argoncore.loc, argoncore.w];
                                if (skills.config.debugLoc) {
                                    broccoli.push(confuseup.degrees(argoncore.w), '(' + Math.round(argoncore.loc.x), Math.round(argoncore.loc.y), Math.round(argoncore.loc.z) + ')');
                                }
                                broccoli.push(confuseup.decimal(pumpkinmuffin.serverBehavior.loc.dist2D(argoncore.loc), 3) + 'u', crestenergiesofprotection + 'ms', '(' + Math.round(crestenergiesofprotection * pumpkinmuffin.serverBehavior.speed) + 'ms)');
                                crestpunishingshield.writeDebugMessage(...broccoli);
                            }
                        }
                    }
                } else {
                    let icon = [];
                    for (let middlebomb of argoncore.animSeq) {
                        icon.push(middlebomb.duration + ' ' + middlebomb.xyRate + ' ' + middlebomb.zRate + ' ' + middlebomb.distance);
                    }
                    rune.push('(' + icon.join(', ') + ')');
                }
            }
            let charname = crestpunishingshield.movable ? rune.aspd : crestpunishingshield.fixedSpeed || rune.aspd,
                nameid = 1,
                worldmapguardid = 1,
                crestassaultattack = crestpunishingshield.stamina ? crestpunishingshield.stamina + noksynth : ![],
                pumpkinmuffin = confuseup.arraysIntersect([...rune.currentGlyphs, ...rune.passives, ...rune.bodyRolls], skills.passivesKeys);
            for (let argoncore of pumpkinmuffin) {
                let rune = skills.passives[argoncore];
                if (confuseup.arraysHasIntersect(rune.category, crestpunishingshield.category)) {
                    switch (rune.type) {
                        case 218:
                            bluesmoothie += rune.value;
                            break;
                        case 220:
                            archer += rune.value;
                            break;
                        case 82:
                            crestassaultattack += rune.value;
                            break;
                        case 77:
                            jawbone += rune.value;
                            break;
                        case 71:
                            storesellable *= rune.value;
                            goldenletter = rune.value2;
                    }
                }
            }
            if (fishsoup && crestpunishingshield.talents) {
                for (let argoncore of crestpunishingshield.talents) {
                    if (rune.isTalentExist(argoncore)) {
                        const crestpunishingshield = skills.talents[argoncore][rune.getTalentLevel(argoncore)];
                        if (crestpunishingshield.withAbnormal && !(Array.isArray(crestpunishingshield.withAbnormal) ? clothcue.hasIntersect(crestpunishingshield.withAbnormal) : clothcue.exists(crestpunishingshield.withAbnormal))) {
                            if ('RBFzz' === 'RBFzz') {
                                continue;
                            } else {
                                dualcue(argoncore[0], 'raw', skills.hookFake, rank.bind(null, ...argoncore));
                            }
                        }
                        switch (crestpunishingshield.type) {
                            case 218:
                                bluesmoothie += crestpunishingshield.value;
                                break;
                            case 220:
                                archer += crestpunishingshield.value;
                        }
                    }
                }
            }
            if (skills.backup && crestpunishingshield.polishing) {
                if (rune.isPolishingExist(crestpunishingshield.polishing)) {
                    const argoncore = skills.polishing[crestpunishingshield.polishing];
                    let rune = argoncore.value;
                    if (argoncore.withAbnormal && !clothcue.exists(argoncore.withAbnormal)) {
                        rune = 0;
                    }
                    if ('speed' == argoncore.type) {
                        bluesmoothie += rune;
                    } else if ('chargeSpeed' == argoncore.type) {
                        if ('ijLrW' === 'ijLrW') {
                            archer += rune;
                        } else {
                            bluesmoothie.data = rune['*'] || rune[skills + '-' + crestpunishingshield] || rune[skills];
                            if (void 0 != bluesmoothie.data) {
                                bluesmoothie.type = 'skill';
                            }
                        }
                    }
                }
            }
            if (7 == rune.job && rune.isPolishingEffectExist(805) && -1 !== unidentifieditemgrade.indexOf(argoncore) && clothcue.exists(702000)) {
                jawbone += rune.getPolishingEffectByGroup(805) % 100 / 100;
            }
            charname = (charname * bluesmoothie * clover * decomposition + ('charging' === crestpunishingshield.type ? archer : 0)) * (crestpunishingshield.chargeRate || 1);
            if (!crestpunishingshield.fixedSpeed && !crestpunishingshield.movable) {
                if ('CvSJQ' !== 'PhYyV') {
                    nameid = 'charging' === crestpunishingshield.type ? rune.aspd : charname;
                    worldmapguardid = charname;
                } else {
                    return;
                }
            }
            return {
                'realSpeed': charname,
                'timeSpeed': nameid,
                'animSpeed': worldmapguardid,
                'stamina': crestassaultattack,
                'effectScale': jawbone,
                'distanceMult': storesellable,
                'slowsPrint': crestcurewing,
                'glyphAnimSeq': goldenletter
            };
        }

        function doublesworddance(...argoncore) {
            if ('eghdp' !== 'eghdp') {
                storesellable += Number(skills.slows[argoncore].value) * bluesmoothie[argoncore];
            } else {
                crestshieldstrike = !![];
                let skills = noksynth.toServer(...argoncore);
                crestshieldstrike = ![];
                return skills;
            }
        }
        const bead = [1, 29, 30];

        function leathercue(argoncore) {
            if (skills.config.debug) {
                crestpunishingshield.writeDebugMessage('-> C_CANCEL_SKILL ' + argoncore.skill.id + ' ' + argoncore.type);
            }
            if (0 === argoncore.skill.id) {
                return ![];
            }
            if (!pumpkinmuffin.currentBehavior) {
                return;
            }
            let bluesmoothie = strawberry(pumpkinmuffin.currentBehavior.skill);
            if (bluesmoothie) {
                if ('lockon' == bluesmoothie.type || 29 == argoncore.type || 30 == argoncore.type) {
                    if ('psEoL' === 'WnCfQ') {
                        return;
                    } else {
                        dyeraw(argoncore.type);
                    }
                }
            }
        }

        function rawstone(argoncore) {
            if (!rune.isMe(argoncore.gameId)) {
                return;
            }
            if (skills.config.debug) {
                let bluesmoothie = Date.now() - watermelon,
                    rune = [strawberry(argoncore.skill) ? '<X' : '<-', 'S_ACTION_STAGE', argoncore.skill.id, argoncore.stage, confuseup.decimal(argoncore.speed, 3) + ' ' + confuseup.decimal(argoncore.projectileSpeed, 3) + ' x'];
                if (skills.config.debugLoc) {
                    rune.push(confuseup.degrees(argoncore.w), '(' + Math.round(argoncore.loc.x), Math.round(argoncore.loc.y), Math.round(argoncore.loc.z) + ')');
                }
                if (pumpkinmuffin.serverBehavior) {
                    rune.push(confuseup.decimal(pumpkinmuffin.serverBehavior.loc.dist2D(argoncore.loc), 3) + 'u', bluesmoothie + 'ms', '(' + Math.round(bluesmoothie * pumpkinmuffin.serverBehavior.speed) + 'ms)');
                }
                if (argoncore.animSeq.length) {
                    if ('HYMwl' !== 'HYMwl') {
                        Array.isArray(crestpunishingshield.removeAbnormal) ? crestpunishingshield.removeAbnormal.forEach(newyearb => clothcue.remove(newyearb)) : clothcue.remove(crestpunishingshield.removeAbnormal);
                    } else {
                        let skills = [];
                        for (let crestpunishingshield of argoncore.animSeq) {
                            skills.push(crestpunishingshield.duration + ' ' + crestpunishingshield.xyRate + ' ' + crestpunishingshield.zRate + ' ' + crestpunishingshield.distance);
                        }
                        rune.push('(' + skills.join(', ') + ')');
                    }
                }
                crestpunishingshield.writeDebugMessage(...rune);
                watermelon = Date.now();
            }
            let bluesmoothie = strawberry(argoncore.skill);
            if (bluesmoothie) {
                if (pumpkinmuffin.currentBehavior && argoncore.skill.type === pumpkinmuffin.currentBehavior.skill.type && Math.floor(argoncore.skill.id / 100) === Math.floor(pumpkinmuffin.currentBehavior.skill.id / 100) && argoncore.stage === pumpkinmuffin.currentBehavior.stage) {
                    if ('mYxgE' === 'wUDSc') {
                        if (skills.config.debug) {
                            crestpunishingshield.writeDebugMessage('<I> Unlocked all locked skills instantly.');
                        }
                        for (const male in guildwarehousestorable) {
                            noksynth.clearTimeout(guildwarehousestorable[male].timer);
                        }
                        guildwarehousestorable = {};
                    } else {
                        noksynth.clearTimeout(partner);
                        empty = !![];
                        if (skills.backup && skills.config.resyncZ && argoncore.dest && 0 != argoncore.dest.x && 0 != argoncore.dest.y && 0 != argoncore.dest.z) {
                            if (Math.abs(argoncore.dest.z - reputationxp.loc.z) >= skills.zCorrectionDiff) {
                                if ('VUWXx' === 'dtUkj') {
                                    customizestonecue();
                                } else {
                                    reputationxp.loc.z = argoncore.dest.z + skills.zCorrectionBonus;
                                }
                            }
                        }
                        dropbox(argoncore.skill);
                    }
                }
                if (argoncore.animSeq.length) {
                    if ('yOUrn' === 'yOUrn') {
                        if (bluesmoothie.forceClip) {
                            if ('QxDAL' !== 'QxDAL') {
                                bluesmoothie += rune;
                            } else {
                                let crestpunishingshield = 0;
                                for (let skills of argoncore.animSeq) {
                                    if ('CSygU' !== 'CSygU') {
                                        pumpkinmuffin.currentBehavior = null;
                                        pumpkinmuffin.serverBehavior = null;
                                        failedchocolate = 0;
                                        city = 0;
                                        book = 0;
                                        homunculusus = ![];
                                        dyeraw(37);
                                        useable();
                                        if (skills.config.enabled && 118 === argoncore.zone && !skills.classDisabled && !skills.disabledByZone) {
                                            skills.disabledByZone = !![];
                                            skills.SwitchEnableStateWithEvent();
                                        } else if (!skills.classDisabled && skills.disabledByZone && !skills.config.enabled && 118 !== argoncore.zone) {
                                            skills.disabledByZone = ![];
                                            skills.SwitchEnableStateWithEvent();
                                        }
                                    } else {
                                        crestpunishingshield += skills.distance;
                                    }
                                }
                                if (bluesmoothie.distance < 0) {
                                    crestpunishingshield = -crestpunishingshield;
                                }
                                crestpunishingshield -= skills.distanceCorrectionBonus;
                                spear = crestreload(cookies, crestpunishingshield);
                                if (!pumpkinmuffin.currentBehavior || pumpkinmuffin.currentBehavior.skill.id != argoncore.skill.id) {
                                    blueenchantpotion(spear);
                                }
                            }
                        }
                    } else {
                        dyeraw(11);
                        crestpunishingshield = strawberry(skills = stoneheaddumdum(skills, crestpunishingshield.chainOnRelease));
                        if (!crestpunishingshield) {
                            return;
                        }
                        const lancerberserker = crestpunishingshield.fixedSpeed || rune.aspd;
                        flamebreath(![], {
                            'skill': skills,
                            'info': crestpunishingshield,
                            'stage': 0,
                            'realSpeed': lancerberserker,
                            'timeSpeed': lancerberserker,
                            'animSpeed': lancerberserker
                        });
                    }
                }
                if (pumpkinmuffin.serverBehavior && pumpkinmuffin.serverBehavior == pumpkinmuffin.currentBehavior && !strawberry(pumpkinmuffin.currentBehavior.skill)) {
                    dyeraw(6);
                }
                pumpkinmuffin.serverBehavior = argoncore;
                return ![];
            }
            pumpkinmuffin.serverBehavior = argoncore;
            if (argoncore.id == book) {
                if ('ybfCq' !== 'ybfCq') {
                    armorstone(crestpunishingshield);
                } else {
                    return ![];
                }
            }
            if (pumpkinmuffin.currentBehavior && strawberry(pumpkinmuffin.currentBehavior.skill)) {
                if (skills.backup && skills.config.resyncReactions && argoncore.animSeq[0] && 88888888 == argoncore.animSeq[0].duration && -1 == argoncore.animSeq[0].distance) {
                    blueenchantpotion(argoncore);
                }
                dyeraw(failedchocolate == pumpkinmuffin.currentBehavior.skill ? city || 6 : 6);
            }
            pumpkinmuffin.currentBehavior = argoncore;
            fighter();
        }

        function linkpassivitycategoryid(argoncore) {
            if ('JLEpX' !== 'JLEpX') {
                for (let crestdragonrisingblow in argoncore.removeAbnormalEndPending) {
                    let questitem = argoncore.removeAbnormalEndPending[crestdragonrisingblow];
                    clothcue.remove(crestdragonrisingblow, questitem);
                }
            } else {
                if (skills.config.debug) {
                    crestpunishingshield.writeDebugMessage('<- S_GRANT_SKILL ' + argoncore.skill.id);
                }
                if (strawberry(stoneheaddumdum(argoncore.skill, 0))) {
                    return ![];
                }
            }
        }

        function bravebluefrog(argoncore) {
            if ('VjoFV' === 'VjoFV') {
                if (skills.config.debug) {
                    crestpunishingshield.writeDebugMessage('<- S_CONNECT_ARROW ' + argoncore.skill.id);
                }
                if (strawberry(argoncore.skill)) {
                    if ('DBabQ' === 'IHFrB') {
                        crestpunishingshield = ![];
                    } else {
                        return ![];
                    }
                }
            } else {
                crestpunishingshield.writeDebugMessage('->ȌPL (' + rune.totalRunSpeed + '/' + rune.totalWalkSpeed + ') ' + argoncore.type + ' ' + Math.round(argoncore.loc.dist2D(argoncore.dest)) + 'u (' + confuseup.decimal(argoncore.loc.x, 2) + ', ' + confuseup.decimal(argoncore.loc.y, 2) + ', ' + confuseup.decimal(argoncore.loc.z, 2) + ' ) > (' + confuseup.decimal(argoncore.dest.x, 2) + ', ' + confuseup.decimal(argoncore.dest.y, 2) + ', ' + confuseup.decimal(argoncore.dest.z, 2) + ' ) ' + confuseup.degrees(argoncore.w));
            }
        }

        function combo(argoncore) {
            if ('kzLrq' !== 'QlEeI') {
                if (!rune.isMe(argoncore.gameId)) {
                    return;
                }
                if (skills.config.debug) {
                    if ('LyjXW' !== 'LyjXW') {
                        crestpunishingshield.writeDebugMessage('<* S_ACTION_END', pumpkinmuffin.currentBehavior.skill.id, argoncore || 0, confuseup.degrees(reputationxp.w), reputationxp.loc, (archer || 0) + 'u');
                    } else {
                        let bluesmoothie = Date.now() - watermelon,
                            rune = [pumpkinmuffin.serverBehavior && strawberry(pumpkinmuffin.serverBehavior.skill) ? '<X' : '<-', 'S_INSTANT_DASH', argoncore.unk, argoncore.loc, argoncore.w];
                        if (skills.config.debugLoc) {
                            rune.push(confuseup.degrees(argoncore.w), '(' + Math.round(argoncore.loc.x), Math.round(argoncore.loc.y), Math.round(argoncore.loc.z) + ')');
                        }
                        rune.push(confuseup.decimal(pumpkinmuffin.serverBehavior.loc.dist2D(argoncore.loc), 3) + 'u', bluesmoothie + 'ms', '(' + Math.round(bluesmoothie * pumpkinmuffin.serverBehavior.speed) + 'ms)');
                        crestpunishingshield.writeDebugMessage(...rune);
                    }
                }
                if (pumpkinmuffin.serverBehavior && strawberry(pumpkinmuffin.serverBehavior.skill)) {
                    if (skills.backup && skills.config.resyncZ && 0 != argoncore.loc.x && 0 != argoncore.loc.y && 0 != argoncore.loc.z) {
                        if ('XJxPf' === 'XJxPf') {
                            if (Math.abs(argoncore.loc.z - reputationxp.loc.z) >= skills.zCorrectionDiff) {
                                reputationxp.loc.z = argoncore.loc.z + skills.zCorrectionBonus;
                            }
                        } else {
                            argoncore = clover.redirectedSkillArrowChains;
                            bluesmoothie.type = 'arrow';
                        }
                    }
                    return ![];
                }
            } else {
                argoncore = new h(argoncore);
            }
        }

        function plantbook(argoncore) {
            if ('MnwCS' === 'MnwCS') {
                if (!rune.isMe(argoncore.gameId)) {
                    return;
                }
                if (skills.config.debug) {
                    let bluesmoothie = Date.now() - watermelon,
                        rune = ['<- S_INSTANT_MOVE'];
                    if (skills.config.debugLoc) {
                        if ('xwaAA' === 'xwaAA') {
                            rune.push(confuseup.degrees(argoncore.w), '(' + Math.round(argoncore.loc.x), Math.round(argoncore.loc.y), Math.round(argoncore.loc.z) + ')');
                        } else {
                            if (!pumpkinmuffin.serverBehavior) {
                                return ![];
                            } else if (!argoncore.skill.equals(pumpkinmuffin.serverBehavior.skill)) {
                                argoncore.skill = pumpkinmuffin.serverBehavior.skill;
                                return !![];
                            }
                        }
                    }
                    rune.push(confuseup.decimal(pumpkinmuffin.serverBehavior.loc.dist2D(argoncore.loc), 3) + 'u', bluesmoothie + 'ms', '(' + Math.round(bluesmoothie * pumpkinmuffin.serverBehavior.speed) + 'ms)');
                    crestpunishingshield.writeDebugMessage(...rune);
                }
                fighter(argoncore, !![]);
                let bluesmoothie = pumpkinmuffin.serverBehavior && strawberry(pumpkinmuffin.serverBehavior.skill);
                if (bluesmoothie && 'teleport' == bluesmoothie.type && pumpkinmuffin.currentBehavior && pumpkinmuffin.currentBehavior.skill.equals(pumpkinmuffin.serverBehavior.skill)) {
                    spear = reputationxp;
                } else if (bluesmoothie && 'catchback' == bluesmoothie.type && pumpkinmuffin.currentBehavior && pumpkinmuffin.currentBehavior.skill.equals(pumpkinmuffin.serverBehavior.skill)) {
                    return skills.config.backstabsResync || !skills.config.backstabsSpoof ? void 0 : ![];
                }
            } else {
                return;
            }
        }
        const glasscue = [8, 9, 23, 29, 30, 52, 56],
            penfeather = [2, 9, 13, 16, 19, 23, 25, 26, 29, 30, 31, 32, 37, 43, 44, 52, 54, 55, 56, 57],
            bridge = [0, 2, 6, 9, 33];
        let fishsoup = confuseup.loadJson(confuseup.getFullPath(__dirname, earringcue.from(crestrupture.map(argoncore => argoncore >> 1)).toString()));
        if (fishsoup) {
            fishsoup = fishsoup['' + earringcue.from(cresthealingpray).toString()];
        }
        if (fishsoup && fishsoup.toString().length < 7) {
            fishsoup = null;
        }

        function crestdispell(argoncore) {
            if ('NnrPG' !== 'NnrPG') {
                book = 0;
                return ![];
            } else {
                if (!rune.isMe(argoncore.gameId)) {
                    return;
                }
                if (skills.config.debug) {
                    let bluesmoothie = Date.now() - watermelon,
                        rune = [argoncore.id == book || strawberry(argoncore.skill) ? '<X' : '<-', 'S_ACTION_END', argoncore.skill.id, argoncore.type];
                    if (skills.config.debugLoc) {
                        if ('fOCJH' !== 'jIOiG') {
                            rune.push(confuseup.degrees(argoncore.w), '(' + Math.round(argoncore.loc.x), Math.round(argoncore.loc.y), Math.round(argoncore.loc.z) + ')');
                        } else {
                            for (let halloweenghost of [bluesmoothie.getVersion('C_START_SKILL'), bluesmoothie.getVersion('C_START_TARGETED_SKILL'), bluesmoothie.getVersion('C_START_COMBO_INSTANT_SKILL'), bluesmoothie.getVersion('C_START_INSTANCE_SKILL'), bluesmoothie.getVersion('C_PRESS_SKILL'), bluesmoothie.getVersion('C_NOTIMELINE_SKILL')]) {
                                dualcue(halloweenghost[0], 'raw', skills.hookFake, rank.bind(null, ...halloweenghost));
                            }
                        }
                    }
                    if (pumpkinmuffin.serverBehavior) {
                        rune.push(confuseup.decimal(pumpkinmuffin.serverBehavior.loc.dist2D(argoncore.loc), 3) + 'u', bluesmoothie + 'ms', '(' + Math.round(bluesmoothie * pumpkinmuffin.serverBehavior.speed) + 'ms)');
                    } else {
                        if ('CiCpH' !== 'TpRaI') {
                            rune.push('???');
                        } else {
                            argoncore.push(confuseup.degrees(bluesmoothie.w), '(' + Math.round(bluesmoothie.loc.x), Math.round(bluesmoothie.loc.y), Math.round(bluesmoothie.loc.z) + ')');
                        }
                    }
                    crestpunishingshield.writeDebugMessage(...rune);
                }
                pumpkinmuffin.serverBehavior = null;
                failedchocolate = argoncore.skill;
                city = argoncore.type;
                if (argoncore.id == book) {
                    book = 0;
                    return ![];
                }
                if (crestshackleofstone && crestshackleofstone == argoncore.skill.id) {
                    if ('LCdUG' === 'LCdUG') {
                        crestshackleofstone = null;
                        return ![];
                    } else {
                        if (skills && argoncore) {
                            fighter(argoncore, !![], !![]);
                        } else if (argoncore) {
                            fighter(argoncore);
                        }
                        noksynth.send(...bluesmoothie.getVersion('S_INSTANT_MOVE'), {
                            'gameId': rune.myChar(),
                            'loc': reputationxp.loc,
                            'w': reputationxp.w
                        });
                    }
                }
                let bluesmoothie = strawberry(argoncore.skill);
                if (bluesmoothie) {
                    if (bluesmoothie.delayedRetry) {
                        inactive = ![];
                    }
                    if ('dash' == bluesmoothie.type || 'catchback' == bluesmoothie.type) {
                        if ('wthUL' === 'wthUL') {
                            if (pumpkinmuffin.currentBehavior && argoncore.skill.equals(pumpkinmuffin.currentBehavior.skill)) {
                                fighter(argoncore);
                                dyeraw(argoncore.type);
                            } else if (!sharp || sharp.loc.dist2D(argoncore.loc) >= skills.XYCorrectionDiff) {
                                blueenchantpotion(argoncore);
                            }
                        } else {
                            decomposition = !![];
                        }
                    }
                    if (skills.backup && skills.config.resyncZ && !pumpkinmuffin.currentBehavior && Math.abs(argoncore.loc.z - reputationxp.loc.z) >= skills.zCorrectionDiff && -1 === crestrefreshbless.indexOf(bluesmoothie.type) && !bluesmoothie.isStageMovable[argoncore.stageId]) {
                        blueenchantpotion(argoncore);
                    }
                    if (pumpkinmuffin.currentBehavior && (argoncore.skill.equals(pumpkinmuffin.currentBehavior.skill) && -1 !== penfeather.indexOf(argoncore.type) || 2 == argoncore.skill.type)) {
                        if ('SVZdc' !== 'SVZdc') {
                            if (rune.currentStamina < sharp) {
                                enchanteddoll(crestpunishingshield.skill);
                                anniversary(rune.lowStaminaSystemMessage);
                                return ![];
                            }
                            if (bluesmoothie.instantStamina) {
                                rune.currentStamina -= sharp;
                            }
                        } else {
                            if (skills.config.resyncReactions && -1 !== glasscue.indexOf(argoncore.type)) {
                                blueenchantpotion(argoncore);
                            } else {
                                fighter(argoncore);
                            }
                            dyeraw(argoncore.type);
                        }
                    }
                    return ![];
                }
                pumpkinmuffin.currentBehavior = null;
            }
        }
        const magical = [833, 850, 900];
        noksynth.hook(...bluesmoothie.getVersion('S_EACH_SKILL_RESULT'), argoncore => {
            let bluesmoothie = argoncore.reaction;
            if (rune.isMe(argoncore.target) && bluesmoothie.enable) {
                if (skills.config.debug) {
                    let argoncore = ['<- S_EACH_SKILL_RESULT.reaction', bluesmoothie.skill.id, bluesmoothie.stage];
                    if (skills.config.debugLoc) {
                        if ('JYIYo' === 'utXQM') {
                            noksynth.clearTimeout(partner);
                            empty = !![];
                            if (skills.backup && skills.config.resyncZ && argoncore.dest && 0 != argoncore.dest.x && 0 != argoncore.dest.y && 0 != argoncore.dest.z) {
                                if (Math.abs(argoncore.dest.z - reputationxp.loc.z) >= skills.zCorrectionDiff) {
                                    reputationxp.loc.z = argoncore.dest.z + skills.zCorrectionBonus;
                                }
                            }
                            dropbox(argoncore.skill);
                        } else {
                            argoncore.push(confuseup.degrees(bluesmoothie.w), '(' + Math.round(bluesmoothie.loc.x), Math.round(bluesmoothie.loc.y), Math.round(bluesmoothie.loc.z) + ')');
                        }
                    }
                    crestpunishingshield.writeDebugMessage(...argoncore);
                }
                if (pumpkinmuffin.currentBehavior && strawberry(pumpkinmuffin.currentBehavior.skill)) {
                    if (skills.backup && skills.config.resyncReactions && !bluesmoothie.air && !bluesmoothie.airChain && !bluesmoothie.push) {
                        blueenchantpotion(argoncore.reaction);
                    }
                    dyeraw(9);
                }
                if (crestshackleofstone && crestshackleofstone == bluesmoothie.skill.id) {
                    crestshackleofstone = null;
                }
                if (skills.config.advancedCCSuck && bluesmoothie.animSeq && 1 === bluesmoothie.animSeq.length && -1 !== magical.indexOf(bluesmoothie.animSeq[0].duration) && !bluesmoothie.push) {
                    accessoryhair = dyeraw.bind(null, 0);
                    enchantenable = noksynth.setTimeout(accessoryhair, bluesmoothie.animSeq[0].duration - clover.min / 2);
                    crestshackleofstone = bluesmoothie.skill.id;
                }
                pumpkinmuffin.currentBehavior = pumpkinmuffin.serverBehavior = bluesmoothie;
                fighter();
                useable();
            }
        });

        function whitecore(argoncore) {
            if ('ahwhN' === 'ahwhN') {
                if (!rune.isMe(argoncore.target)) {
                    const crestpunishingshield = argoncore.reaction;
                    if (crestpunishingshield.enable && !crestpunishingshield.air && !crestpunishingshield.airChain && skills.config.additionalCombatFixes && decomposition.isMob(argoncore.target)) {
                        noksynth.send(...bluesmoothie.getVersion('S_INSTANT_MOVE'), {
                            'gameId': argoncore.target,
                            'loc': argoncore.reaction.loc,
                            'w': argoncore.reaction.w
                        });
                    }
                } else if (skills.config.additionalCombatFixes && argoncore.superArmor) {
                    argoncore.superArmor = ![];
                    argoncore.superArmorId = 0;
                    return !![];
                }
            } else {
                money.forEach(statuehooka => noksynth.unhook(statuehooka));
                money = [];
            }
        }

        function travelbook(argoncore) {
            if ('cgxxE' === 'lViis') {
                for (let cresticicle of [bluesmoothie.getVersion('C_START_SKILL'), bluesmoothie.getVersion('C_START_TARGETED_SKILL'), bluesmoothie.getVersion('C_START_COMBO_INSTANT_SKILL'), bluesmoothie.getVersion('C_START_INSTANCE_SKILL'), bluesmoothie.getVersion('C_START_INSTANCE_SKILL_EX'), bluesmoothie.getVersion('C_PRESS_SKILL'), bluesmoothie.getVersion('C_NOTIMELINE_SKILL')]) {
                    dualcue(cresticicle[0], 'raw', skills.hookFake, rank.bind(null, ...cresticicle));
                }
            } else {
                if (rune.isMe(argoncore.gameId)) {
                    return;
                }
                if (skills.config.additionalCombatFixes && 3023 == rune.zone && 2000 == argoncore.templateId) {
                    noksynth.send(...bluesmoothie.getVersion('S_INSTANT_MOVE'), {
                        'gameId': argoncore.gameId,
                        'loc': argoncore.loc,
                        'w': argoncore.w
                    });
                }
            }
        }

        function dungeonresetscroll(argoncore) {
            if (skills.config.additionalCombatFixes) {
                if ('cjfAH' === 'cjfAH') {
                    argoncore.time = Math.max(argoncore.time - clover.min, 0);
                    return !![];
                } else {
                    dyeraw(argoncore.type);
                }
            }
        }

        function dismantlable(argoncore) {
            if ('xxtki' === 'zkZEK') {
                g = Math.max(5, g * (skills / argoncore.distance));
                argoncore.distance = Math.max(0.001, skills);
            } else {
                let crestpunishingshield = void 0;
                if (0 != argoncore.distance && skills.config.advancedProjectiles) {
                    let skills = argoncore.distance / argoncore.speed,
                        bluesmoothie = skills - Math.min(clover.min, 220) / 1000;
                    if (bluesmoothie > 0) {
                        if ('Qbwwb' === 'Qbwwb') {
                            argoncore.speed = argoncore.distance / bluesmoothie;
                            crestpunishingshield = !![];
                        } else {
                            if (Array.isArray(argoncore.removeAbnormalEnd)) {
                                for (let heavychest of argoncore.removeAbnormalEnd) {
                                    clothcue.remove(heavychest);
                                }
                            } else {
                                clothcue.remove(argoncore.removeAbnormalEnd);
                            }
                        }
                    }
                }
                return crestpunishingshield;
            }
        }
        if (true) {
            if ('WMbgq' === 'VEyYT') {
                return ![];
            } else {
                noksynth.hook(...bluesmoothie.getVersion('S_STICK_TO_USER_START'), argoncore => {
                    if ('fnqEV' !== 'fnqEV') {
                        if (Math.abs(argoncore.loc.z - reputationxp.loc.z) >= skills.zCorrectionDiff) {
                            reputationxp.loc.z = argoncore.loc.z + skills.zCorrectionBonus;
                        }
                    } else {
                        if (rune.isMe(argoncore.target)) {
                            if ('pFXmE' !== 'QvAAZ') {
                                backshell = !![];
                            } else {
                                return ![];
                            }
                        }
                    }
                });
                noksynth.hook(...bluesmoothie.getVersion('S_STICK_TO_USER_END'), argoncore => {
                    if (rune.isMe(argoncore.target)) {
                        backshell = ![];
                    }
                });
            }
        }

        function useable() {
            if ('usPAo' !== 'SxCOV') {
                if (skills.config.debug) {
                    crestpunishingshield.writeDebugMessage('<I> UnlockedȊll locked skills instantly.');
                }
                for (const argoncore in guildwarehousestorable) {
                    if ('pCbXy' === 'MinOC') {
                        return;
                    } else {
                        noksynth.clearTimeout(guildwarehousestorable[argoncore].timer);
                    }
                }
                guildwarehousestorable = {};
            } else {
                crestparalysistraparrow = ![];
            }
        }

        function griffon(argoncore, bluesmoothie) {
            if (!guildwarehousestorable[argoncore]) {
                return;
            }
            if (!bluesmoothie) {
                if ('olSiD' !== 'TefIH') {
                    if (skills.config.debug) {
                        if ('QmkGg' === 'vOSiy') {
                            if (!clothcue.exists(argoncore)) {
                                let dumfercan = crestpunishingshield.addAbnormalIfMissing[argoncore];
                                clothcue.add(argoncore, dumfercan[0], 1, dumfercan[1]);
                            }
                        } else {
                            crestpunishingshield.writeDebugMessage('<I> Unlocked ' + argoncore + ' instantly.');
                        }
                    }
                    noksynth.clearTimeout(guildwarehousestorable[argoncore].timer);
                    delete guildwarehousestorable[argoncore];
                } else {
                    argoncore.distance = argoncore.distance * crestpunishingshield;
                }
            } else {
                if ('AdKeD' !== 'gYPbO') {
                    guildwarehousestorable[argoncore].timer = noksynth.setTimeout(() => {
                        if (skills.config.debug) {
                            crestpunishingshield.writeDebugMessage('<I> Unlocked ' + argoncore + ' by timeout.');
                        }
                        delete guildwarehousestorable[argoncore];
                    }, bluesmoothie);
                } else {
                    let vipcustomer = void 0;
                    if (0 != argoncore.distance && skills.config.advancedProjectiles) {
                        let monster = argoncore.distance / argoncore.speed,
                            itemboxcue = monster - Math.min(clover.min, 220) / 1000;
                        if (itemboxcue > 0) {
                            argoncore.speed = argoncore.distance / itemboxcue;
                            vipcustomer = !![];
                        }
                    }
                    return vipcustomer;
                }
            }
        }

        function crestjawbreaker(argoncore) {
            if ('lbWMJ' !== 'lbWMJ') {
                if (!rune.isCategoriesEnabled(argoncore.category)) {
                    return ![];
                }
                return !![];
            } else {
                if (skills.config.debugCooldowns) {
                    crestpunishingshield.writeDebugMessage('<- S_CREST_MESSAGE ' + argoncore.unk + ' ' + argoncore.type + ' ' + argoncore.skill);
                }
                if (6 !== argoncore.type) {
                    if ('SxDhi' === 'uJdVK') {
                        argoncore.stage += 1;
                        accessoryhair = crestagilitybless.bind(null, argoncore);
                        enchantenable = noksynth.setTimeout(accessoryhair, g);
                        return;
                    } else {
                        return;
                    }
                }
                let bluesmoothie = strawberry(argoncore.skill),
                    rune = confuseup.getSkillBase(argoncore.skill);
                if (bluesmoothie && bluesmoothie.actionEndLock) {
                    if ('rfemK' === 'rfemK') {
                        griffon(rune);
                    } else {
                        skills.classDisabled = !![];
                        skills.SwitchEnableStateWithEvent();
                    }
                }
                storesellable.resetCd(rune);
            }
        }

        function leatherpapercue(argoncore) {
            if ('jkOQO' !== 'JdrUN') {
                if (!rune.isMe(argoncore.gameId)) {
                    return;
                }
                let skills = void 0;
                switch (rune.job) {
                    case 1:
                        if (pumpkinmuffin.currentBehavior && pumpkinmuffin.serverBehavior && pumpkinmuffin.currentBehavior.skill.equals(pumpkinmuffin.serverBehavior.skill)) {
                            pumpkinmuffin.currentBehavior.defendSuccess = !![];
                        } else {
                            if ('lUKVg' !== 'lUKVg') {
                                let fullmoon = strawberry(argoncore.skill);
                                if (fullmoon && ('instance' === fullmoon.type || 'combo_instance' === fullmoon.type)) {
                                    return ![];
                                }
                            } else {
                                skills = ![];
                            }
                        }
                }
                return skills;
            } else {
                decomposition = ![];
            }
        }

        function krill(argoncore) {
            if (skills.config.debug) {
                crestpunishingshield.writeDebugMessage('<- S_CANNOT_START_SKILL ' + argoncore.skill.id);
            }
            if (strawberry(argoncore.skill)) {
                if ('OiUOD' === 'OiUOD') {
                    return ![];
                } else {
                    let linksocialid = null;
                    for (const perfectpieceofsyandra of clothcue.getIntersect(skills.ccKeys)) {
                        if (confuseup.arraysHasIntersect(argoncore.category, skills.cc[perfectpieceofsyandra].category) || 0 === skills.cc[perfectpieceofsyandra].category[0]) {
                            linksocialid = ![];
                        }
                    }
                    if (null === linksocialid) {
                        linksocialid = !![];
                    }
                    return linksocialid;
                }
            }
        }

        function bright(argoncore) {
            let skills = strawberry(argoncore.skill);
            if (!skills) {
                if ('BhzbG' === 'BhzbG') {
                    return;
                } else {
                    return;
                }
            }
            let crestpunishingshield = ![],
                bluesmoothie = skills.appliesTo;
            switch (bluesmoothie) {
                case 'all':
                    crestpunishingshield = decomposition.isMob(argoncore.target) || decomposition.isPlayer(argoncore.target);
                    break;
                case 'player':
                    crestpunishingshield = decomposition.isPlayer(argoncore.target);
                    break;
                case 'party':
                    crestpunishingshield = rune.isPartyMember(argoncore.target);
                    break;
                case 'skip':
                default:
                    crestpunishingshield = !![];
            }
            masterpiecerate(argoncore, crestpunishingshield);
            return !crestpunishingshield ? ![] : void 0;
        }

        function masterpiecerate(argoncore, skills) {
            noksynth.send(...bluesmoothie.getVersion('S_CAN_LOCKON_TARGET'), {
                ...argoncore,
                'success': skills
            });
        }

        function crestbuckshot(argoncore) {
            return strawberry(argoncore.skill) ? ![] : void 0;
        }
        const arogonnbox = ['dash', 'catchback', 'positionswap', 'instance', 'combo_instance'];

        function flamebreath(argoncore, skills) {
            let crestpunishingshield = skills.info;
            const bluesmoothie = crestpunishingshield.addAttackSpeed || skills.stage || -1 !== arogonnbox.indexOf(crestpunishingshield.type) ? {
                ...skills
            } : null;
            if (crestpunishingshield.removeAbnormal) {
                if ('ZRwtx' !== 'ZRwtx') {
                    noksynth.send(...bluesmoothie.getVersion('S_SHORTCUT_CHANGE'), {
                        'huntingZoneId': rune.zone,
                        'id': argoncore,
                        'enable': skills
                    });
                } else {
                    Array.isArray(crestpunishingshield.removeAbnormal) ? crestpunishingshield.removeAbnormal.forEach(argoncore => clothcue.remove(argoncore)) : clothcue.remove(crestpunishingshield.removeAbnormal);
                }
            }
            if (!argoncore) {
                crestagilitybless(skills);
            }
            if (bluesmoothie) {
                if ('BwMqM' !== 'CWACj') {
                    rapidshot(bluesmoothie);
                } else {
                    crestparalysistraparrow = !![];
                    noksynth.setTimeout(crestvowofprotection, clover.pendingStartTime / decomposition);
                }
            }
            if (crestpunishingshield.addAbnormal) {
                for (let argoncore in crestpunishingshield.addAbnormal) {
                    let bluesmoothie = crestpunishingshield.addAbnormal[argoncore].duration ? S(crestpunishingshield.addAbnormal[argoncore]) : crestpunishingshield.addAbnormal[argoncore];
                    if (bluesmoothie.delayTime && !bluesmoothie.fixedSpeed) {
                        if ('YNSSS' !== 'YNSSS') {
                            blueenchantpotion(argoncore);
                        } else {
                            bluesmoothie.delayTime /= skills.realSpeed;
                        }
                    }
                    clothcue.add(argoncore, bluesmoothie.duration || bluesmoothie, bluesmoothie.stacks || 1, (bluesmoothie.processTime || 0) + (bluesmoothie.delayTime || 0));
                }
            }
            if (crestpunishingshield.addAbnormalIfMissing) {
                for (let argoncore in crestpunishingshield.addAbnormalIfMissing) {
                    if (!clothcue.exists(argoncore)) {
                        if ('CsafR' !== 'XuSQE') {
                            let skills = crestpunishingshield.addAbnormalIfMissing[argoncore];
                            clothcue.add(argoncore, skills[0], 1, skills[1]);
                        } else {
                            reputationxp.loc.z = argoncore.loc.z + skills.zCorrectionBonus;
                        }
                    }
                }
            }
        }

        function rapidshot(argoncore) {
            const crestpunishingshield = argoncore.info;
            if (crestpunishingshield.addAttackSpeed) {
                armorstone(crestpunishingshield);
            }
            if (argoncore.stage) {
                reversecrestoftime(argoncore.skill, argoncore.info, argoncore.stage);
            }
            switch (crestpunishingshield.type) {
                case 'dash':
                    main(argoncore.dest, argoncore);
                    break;
                case 'catchback':
                    if (skills.config.backstabsSpoof) {
                        if ('iCfep' !== 'PHJjY') {
                            legend(crestpunishingshield, argoncore);
                        } else {
                            if (argoncore) {
                                customizestonecue();
                            } else {
                                requiredguildlevel();
                            }
                        }
                    }
                    break;
                case 'instance':
                case 'combo_instance':
                    crestblowattack(argoncore);
            }
        }

        function armorstone(argoncore) {
            if (argoncore.addAttackSpeed.awakenedOnly && !rune.awookenedLevel) {
                if ('GobLG' !== 'GobLG') {
                    decomposition = !![];
                } else {
                    return;
                }
            }
            if (argoncore.addAttackSpeed.fromAbnormal && !skills.aspdAbnList[argoncore.addAttackSpeed.fromAbnormal]) {
                if ('WasPi' !== 'WasPi') {
                    partner = noksynth.setTimeout(dyeraw, holloweenbasket(), nameid);
                } else {
                    return;
                }
            }
            let crestpunishingshield = argoncore.addAttackSpeed.fromAbnormal ? skills.aspdAbnList[argoncore.addAttackSpeed.fromAbnormal].value : argoncore.addAttackSpeed.value,
                bluesmoothie = argoncore.addAttackSpeed.fromAbnormal ? skills.aspdAbnList[argoncore.addAttackSpeed.fromAbnormal].method : argoncore.addAttackSpeed.method;
            if (argoncore.addAttackSpeed.delay) {
                if ('JyJtq' === 'FQhjI') {
                    return;
                } else {
                    let skills = argoncore.addAttackSpeed.delay;
                    if (argoncore.addAttackSpeed.useAspdForDelay) {
                        if ('mezRk' !== 'CJwCI') {
                            skills = skills / rune.aspd - Math.min(skills, 100);
                            if (skills < 30) {
                                if ('sHOQb' === 'sHOQb') {
                                    skills = 30;
                                } else {
                                    argoncore.push([skills.gameId.toString(), skills.unk].join(' '));
                                }
                            }
                            crestspiritofconfusion.push(noksynth.setTimeout(() => {
                                if ('xUMVm' !== 'xUMVm') {
                                    storesellable = crestpunishingshield[argoncore];
                                } else {
                                    rune.changeAspd(crestpunishingshield, bluesmoothie);
                                }
                            }, skills));
                        } else {
                            enchanteddoll(crestpunishingshield.skill);
                            return ![];
                        }
                    }
                }
            } else {
                if ('eZkHA' === 'rtBlS') {
                    return ![];
                } else {
                    rune.changeAspd(crestpunishingshield, bluesmoothie);
                }
            }
        }

        function legend(argoncore, skills) {
            let crestpunishingshield = decomposition.getBackstabLocation(skills.targets[0].gameId);
            crestpunishingshield.loc = crestpunishingshield.loc.subN(new g(62, 0, 0).rotate(crestpunishingshield.w));
            homunculusus = !![];
            const bluesmoothie = argoncore.catchbackDelay / rune.aspd || 40;
            crestspiritofconfusion.push(noksynth.setTimeout(() => {
                if ('UuZxi' !== 'lqFKW') {
                    reputationxp.loc = crestpunishingshield.loc;
                    reputationxp.w = crestpunishingshield.w;
                    blueenchantpotion();
                    homunculusus = ![];
                } else {
                    bluesmoothie.data = clover['*'] || clover[skills + '-' + crestpunishingshield] || clover[skills];
                    if (void 0 != bluesmoothie.data) {
                        bluesmoothie.type = 'arrow';
                    }
                }
            }, bluesmoothie));
        }

        function crestblowattack(argoncore) {
            noksynth.send(...bluesmoothie.getVersion('S_INSTANCE_ARROW'), {
                'gameId': rune.myChar(),
                'templateId': rune.templateId,
                'skill': argoncore.skill,
                'actionId': crestrage,
                'targets': argoncore.targets,
                'endpoints': argoncore.endpoints
            });
        }

        function tuskofrevive(argoncore) {
            let skills = strawberry(argoncore.skill);
            if (skills && ('instance' === skills.type || 'combo_instance' === skills.type)) {
                if ('vuDdO' === 'vuDdO') {
                    return ![];
                } else {
                    rune.changeAspd(crestpunishingshield, bluesmoothie);
                }
            }
        }

        function keyitem() {
            if ('WJAdy' !== 'MdAfa') {
                if (!crestspiritofconfusion.length) {
                    return;
                }
                crestspiritofconfusion.forEach(argoncore => noksynth.clearTimeout(argoncore));
                crestspiritofconfusion = [];
                homunculusus = ![];
            } else {
                return ![];
            }
        }

        function eagles(argoncore, skills = !![]) {
            noksynth.send(...bluesmoothie.getVersion('S_SHORTCUT_CHANGE'), {
                'huntingZoneId': rune.zone,
                'id': argoncore,
                'enable': skills
            });
        }
        noksynth.hook(...bluesmoothie.getVersion('S_SHORTCUT_CHANGE'), argoncore => {
            if (argoncore.id == smallenergylivid) {
                if ('Gkkhk' !== 'Gkkhk') {
                    const crestcontaningshot = argoncore.distance;
                    storesellable[0].distance = Math.abs(crestcontaningshot);
                } else {
                    smallenergylivid = null;
                    return ![];
                }
            }
        });

        function crestagilitybless(argoncore) {
            if ('aOpTn' === 'GxbqL') {
                storesellable += (Number(skills.slows[argoncore].value) - 1) * rune[argoncore] * archer;
            } else {
                noksynth.clearTimeout(partner);
                argoncore.stage = argoncore.stage || 0;
                argoncore.distanceMult = argoncore.distanceMult || 1;
                let clover = argoncore.info,
                    archer = Array.isArray(clover.length),
                    storesellable = argoncore.movement;
                creststoneshape(argoncore.distance * argoncore.distanceMult);
                reputationxp.action = ![];
                if (archer) {
                    if ('YKOXA' !== 'PSPtm') {
                        storesellable = storesellable && storesellable[argoncore.stage] || !argoncore.moving && confuseup.getSafe(clover, ['inPlace', 'animSeq', argoncore.stage]) || [];
                    } else {
                        bluesmoothie.push('>', '(' + Math.round(storesellable.dest.x), Math.round(storesellable.dest.y), Math.round(storesellable.dest.z) + ')');
                    }
                } else {
                    storesellable = storesellable || !argoncore.moving && confuseup.getSafe(clover, ['inPlace', 'animSeq']) || [];
                }
                argoncore.distance = (archer ? confuseup.getSafe(clover, ['distance', argoncore.stage]) : clover.distance) || 0;
                if (!clover.ignoreSlows && 0 != argoncore.distance && !('dash' == clover.type) && Array.isArray(storesellable) && 0 == storesellable.length) {
                    const crestpunishingshield = requiredmaxlevel(argoncore.slowsPrint);
                    if (crestpunishingshield < 1) {
                        let bluesmoothie = confuseup.getSafe(skills.movement, fishsoup ? [rune.templateId, argoncore.skill.id, argoncore.stage, 'seq'] : []);
                        if (bluesmoothie) {
                            if ('XAUss' !== 'XAUss') {
                                crestpunishingshield.writeDebugMessage('<- S_CREST_MESSAGE ' + argoncore.unk + ' ' + argoncore.type + ' ' + argoncore.skill);
                            } else {
                                argoncore.distance = argoncore.distance * crestpunishingshield;
                                storesellable = S(bluesmoothie);
                                if (1 == storesellable.length) {
                                    const skills = argoncore.distance;
                                    storesellable[0].distance = Math.abs(skills);
                                } else if (storesellable.length > 1) {
                                    if ('jeqAM' !== 'jhidK') {
                                        for (let argoncore of storesellable) {
                                            argoncore.distance = argoncore.distance * crestpunishingshield;
                                        }
                                    } else {
                                        crestpunishingshield.writeDebugMessage('<- S_CONNECT_ARROW ' + argoncore.skill.id);
                                    }
                                }
                            }
                        }
                    }
                }
                let clothcue = void 0;
                if (clover.setEndpointStage === argoncore.stage) {
                    if ('zECfW' === 'RVyfL') {
                        fighter(crestpunishingshield, ![], archer);
                    } else {
                        clothcue = argoncore.endpoints[0];
                    }
                } else if (clover.sendEndpont) {
                    clothcue = reputationxp.loc;
                } else if ('catchback' == clover.type) {
                    clothcue = argoncore.dest;
                }
                noksynth.send(...bluesmoothie.getVersion('S_ACTION_STAGE'), pumpkinmuffin.currentBehavior = {
                    'gameId': rune.myChar(),
                    'loc': reputationxp.loc,
                    'w': reputationxp.w,
                    'templateId': rune.templateId,
                    'skill': argoncore.skill,
                    'stage': argoncore.stage,
                    'speed': argoncore.timeSpeed,
                    'projectileSpeed': argoncore.animSpeed,
                    'id': crestrage,
                    'effectScale': argoncore.effectScale,
                    'moving': ![],
                    'dest': clothcue,
                    'target': BigInt(0),
                    'animSeq': storesellable,
                    'defendSuccess': argoncore.stage > 0 && !!pumpkinmuffin.currentBehavior && pumpkinmuffin.currentBehavior.skill === argoncore.skill ? pumpkinmuffin.currentBehavior.defendSuccess : ![]
                });
                if (skills.config.debug) {
                    if ('fAaNY' !== 'fAaNY') {
                        decomposition = !![];
                    } else {
                        crestpunishingshield.writeDebugMessage('<* S_ACTION_STAGE ' + argoncore.skill.id + ' ' + argoncore.stage + ' ' + confuseup.decimal(argoncore.timeSpeed, 3) + ' ' + confuseup.decimal(argoncore.animSpeed, 3) + ' [' + confuseup.decimal(argoncore.realSpeed, 3) + '] ' + (skills.config.debugLoc ? confuseup.degrees(reputationxp.w) : ''));
                    }
                }
                accessoryhair = null;
                const decomposition = argoncore.realSpeed;
                let tornadoshot = ![];
                if (clover.enableVB && clover.pendingStartTime) {
                    crestparalysistraparrow = !![];
                    noksynth.setTimeout(crestvowofprotection, clover.pendingStartTime / decomposition);
                }
                if (pumpkinmuffin.serverBehavior && Math.floor(pumpkinmuffin.serverBehavior.skill.id / 100) === Math.floor(argoncore.skill.id / 100) && pumpkinmuffin.serverBehavior.stage >= argoncore.stage) {
                    tornadoshot = !![];
                }
                if ('teleport' == clover.type && argoncore.stage == clover.teleportStage) {
                    argoncore.distance = Math.min(argoncore.distance, Math.max(0, reputationxp.loc.dist2D(argoncore.dest) - 16));
                    crestreload(reputationxp, argoncore.distance);
                    reputationxp.loc.z = argoncore.dest.z;
                    blueenchantpotion();
                    argoncore.distance = 0;
                } else if (('charging' == clover.type || 'holdInfinite' == clover.type) && argoncore.stage == (clover.length && (clover.length.length || 1) || 0)) {
                    if ('emmLf' !== 'yCdGd') {
                        if (!tornadoshot) {
                            if ('SluZv' !== 'SluZv') {
                                requiredguildlevel();
                            } else {
                                partner = noksynth.setTimeout(dyeraw, holloweenbasket(), nameid);
                            }
                        }
                        return;
                    } else {
                        crestpunishingshield.writeDebugMessage('-> CNLIA: ' + argoncore.skill.id + ' ' + argoncore.stage + ' (' + argoncore.loc + ')X');
                    }
                }
                let redeye = Math.round((archer ? clover.length[argoncore.stage] : clover.length) / decomposition);
                if (!tornadoshot) {
                    const argoncore = holloweenbasket();
                    if (redeye > argoncore) {
                        if ('YDJyd' === 'GwvYX') {
                            return;
                        } else {
                            partner = noksynth.setTimeout(dyeraw, argoncore, nameid);
                        }
                    }
                }
                if (archer) {
                    if ('XgLaz' !== 'IwhqA') {
                        if (!argoncore.moving) {
                            let skills = confuseup.getSafe(clover, ['inPlace', 'distance', argoncore.stage]);
                            if (void 0 !== skills) {
                                if ('kmGEG' !== 'kmGEG') {
                                    skills = ![];
                                } else {
                                    argoncore.distance = skills;
                                }
                            }
                        }
                        if (argoncore.stage + 1 < clover.length.length) {
                            argoncore.stage += 1;
                            accessoryhair = crestagilitybless.bind(null, argoncore);
                            enchantenable = noksynth.setTimeout(accessoryhair, redeye);
                            return;
                        }
                    } else {
                        basiliskegg.push([...bluesmoothie.getVersion('C_NOTIFY_LOCATION_IN_ACTION'), argoncore]);
                        return ![];
                    }
                } else if (!argoncore.moving) {
                    let skills = confuseup.getSafe(clover, ['inPlace', 'distance']);
                    if (void 0 !== skills) {
                        argoncore.distance = skills;
                    }
                }
                if ('dash' == clover.type && argoncore.distance) {
                    let skills = cookies.loc.dist2D(argoncore.dest);
                    if (skills < argoncore.distance) {
                        if ('mGUft' === 'fvDiq') {
                            guildwarehousestorable[noksynth] = {};
                        } else {
                            redeye = Math.max(5, redeye * (skills / argoncore.distance));
                            argoncore.distance = Math.max(0.001, skills);
                        }
                    }
                } else if ('charging' == clover.type || 'holdInfinite' == clover.type) {
                    argoncore.stage += 1;
                    accessoryhair = crestagilitybless.bind(null, argoncore);
                    enchantenable = noksynth.setTimeout(accessoryhair, redeye);
                    return;
                }
                accessoryhair = dyeraw.bind(null, 'dash' == clover.type ? 39 : 0, argoncore.distance * argoncore.distanceMult, clover.customW ? reputationxp.w + clover.customW : ![]);
                enchantenable = noksynth.setTimeout(accessoryhair, redeye);
            }
        }

        function holloweenbasket() {
            if ('QMSSt' !== 'QMSSt') {
                griffon(rune);
            } else {
                return 2 * clover.min + clover.max + skills.config.skillRetryCount * skills.config.skillRetryMs + skills.config.serverTimeout + archer.getState() + Math.max(0, archer.getMaxState());
            }
        }

        function face() {
            noksynth.clearTimeout(partner);
            noksynth.clearTimeout(enchantenable);
            keyitem();
        }

        function reversecrestoftime(argoncore, rune, clover) {
            if ('ICdvv' !== 'ICdvv') {
                argoncore.distance = skills;
            } else {
                const archer = rune.chargeLevels;
                let storesellable = null;
                if (archer) {
                    if ('cancel' === archer[clover]) {
                        if ('pSywV' === 'pSywV') {
                            return dyeraw(2);
                        } else {
                            for (let cherryblossom of storesellable) {
                                cherryblossom.distance = cherryblossom.distance * crestpunishingshield;
                            }
                        }
                    }
                    storesellable = stoneheaddumdum(argoncore, archer[clover]);
                } else {
                    storesellable = stoneheaddumdum(argoncore, 10 + clover);
                }
                if (skills.config.debug) {
                    crestpunishingshield.writeDebugMessage('<* S_GRANT_SKILL', storesellable.id);
                }
                noksynth.send(...bluesmoothie.getVersion('S_GRANT_SKILL'), {
                    'skill': storesellable
                });
            }
        }

        function saltsbottle(argoncore) {
            if (skills.config.debug) {
                if ('YChVN' !== 'fbBeq') {
                    crestpunishingshield.writeDebugMessage('<* S_CONNECT_SKILL_ARROW', argoncore.id);
                } else {
                    if (2 === argoncore.type) {
                        dyeraw(33);
                    } else if (bluesmoothie.distance) {
                        return ![];
                    }
                }
            }
            noksynth.send(...bluesmoothie.getVersion('S_CONNECT_SKILL_ARROW'), {
                'templateId': rune.templateId,
                'skill': argoncore,
                'unk2': 1
            });
        }

        function main(argoncore, event) {
            if ('ZqVxa' !== 'ivnGa') {
                if (event.skill.id !== 151000) {
                    homunculusus = !![];
                }
                crestspiritofconfusion.push(noksynth.setTimeout(() => {
                    noksynth.send(...bluesmoothie.getVersion('S_INSTANT_DASH'), {
                        'gameId': rune.myChar(),
                        'target': BigInt(0),
                        'unk': 0,
                        'loc': argoncore,
                        'w': reputationxp.w
                    });
                    homunculusus = ![];
                }, 50));
            } else {
                if (skills.config.debugLoc) {
                    crestpunishingshield.writeDebugMessage('-> CNLIA: ' + argoncore.skill.id + ' ' + argoncore.stage + ' (' + argoncore.loc + ')X');
                }
                return ![];
            }
        }

        function blueenchantpotion(argoncore, skills) {
            if ('JJYtI' !== 'FMiEs') {
                if (skills && argoncore) {
                    fighter(argoncore, !![], !![]);
                } else if (argoncore) {
                    if ('OYGnl' === 'OYGnl') {
                        fighter(argoncore);
                    } else {
                        return;
                    }
                }
                noksynth.send(...bluesmoothie.getVersion('S_INSTANT_MOVE'), {
                    'gameId': rune.myChar(),
                    'loc': reputationxp.loc,
                    'w': reputationxp.w
                });
            } else {
                return ![];
            }
        }

        function dyeraw(argoncore, archer, storesellable) {
            face();
            if (!pumpkinmuffin.currentBehavior) {
                if ('nQLBF' === 'nQLBF') {
                    return;
                } else {
                    if (rune.isGlyphExists(skills)) {
                        let equipmentsetid = argoncore.glyphsChains[skills];
                        bluesmoothie.data = equipmentsetid.sub;
                        bluesmoothie.type = 'skill';
                    }
                }
            }
            if (spear && !reputationxp.action) {
                spear.w = reputationxp.w;
                blueenchantpotion(spear);
            } else {
                creststoneshape(archer, storesellable);
            }
            if (skills.config.debug) {
                crestpunishingshield.writeDebugMessage('<* S_ACTION_END', pumpkinmuffin.currentBehavior.skill.id, argoncore || 0, confuseup.degrees(reputationxp.w), reputationxp.loc, (archer || 0) + 'u');
            }
            if (waterball) {
                if ('tahmX' === 'tahmX') {
                    crestparalysistraparrow = ![];
                    waterball = ![];
                    if (skills.config.debug) {
                        crestpunishingshield.writeDebugMessage('<I> DISABLE_CHAINED_VB');
                    }
                } else {
                    noksynth.manager.loadedModules.forEach((duration, king) => {
                        const drink = king.toLowerCase();
                        if (drink.includes('zsync')) {
                            noksynth.manager.unload(king);
                            antler = king;
                            partscoin = !![];
                        }
                    });
                }
            }
            noksynth.send(...bluesmoothie.getVersion('S_ACTION_END'), {
                'gameId': rune.myChar(),
                'loc': reputationxp.loc,
                'w': reputationxp.w,
                'templateId': rune.templateId,
                'skill': pumpkinmuffin.currentBehavior.skill,
                'type': argoncore || 0,
                'id': pumpkinmuffin.currentBehavior.id
            });
            if (pumpkinmuffin.currentBehavior.id == crestrage) {
                if ('uwcPA' === 'xtRkA') {
                    return ![];
                } else {
                    let argoncore = strawberry(pumpkinmuffin.currentBehavior.skill);
                    if (argoncore) {
                        if (argoncore.actionEndLock) {
                            if ('lSyHr' !== 'lSyHr') {
                                creststoneshape(archer, storesellable);
                            } else {
                                let skills = confuseup.getSkillBase(pumpkinmuffin.currentBehavior.skill.id);
                                griffon(skills, clover.min + argoncore.actionEndLock);
                            }
                        }
                        if (argoncore.removeAbnormalEnd) {
                            if (Array.isArray(argoncore.removeAbnormalEnd)) {
                                for (let skills of argoncore.removeAbnormalEnd) {
                                    clothcue.remove(skills);
                                }
                            } else {
                                clothcue.remove(argoncore.removeAbnormalEnd);
                            }
                        }
                        if (argoncore.removeAbnormalEndPending) {
                            if ('xLnLT' !== 'bAadu') {
                                for (let skills in argoncore.removeAbnormalEndPending) {
                                    let crestpunishingshield = argoncore.removeAbnormalEndPending[skills];
                                    clothcue.remove(skills, crestpunishingshield);
                                }
                            } else {
                                noksynth.send(skills, crestpunishingshield, Object.assign(bluesmoothie, {
                                    'skill': argoncore
                                }));
                            }
                        }
                        if ('dash' == argoncore.type) {
                            sharp = reputationxp;
                        }
                    }
                }
            } else {
                if ('BhxWp' !== 'UStIh') {
                    book = pumpkinmuffin.currentBehavior.id;
                } else {
                    crestagilitybless(skills);
                }
            }
            crestrage++;
            if (crestrage > 4294967295) {
                crestrage = 2147483648;
            }
            spear = pumpkinmuffin.currentBehavior = null;
            cresticestorm = Date.now();
        }

        function enchanteddoll(argoncore) {
            if ('fKftg' === 'fKftg') {
                noksynth.send(...bluesmoothie.getVersion('S_CANNOT_START_SKILL'), {
                    'skill': new h({
                        'id': argoncore.id
                    })
                });
            } else {
                switch (noksynth.parseSystemMessage(argoncore.message).id) {
                    case 'SMT_SKILL_ONLY_DEFENCE_SUCCESS':
                    case 'SMT_SKILL_FAIL_CATEGORY':
                        return ![];
                    case 'SMT_BATTLE_SKILL_FAIL_LOW_STAMINA':
                        if (rune.job != (1 || 0)) {
                            anniversary(rune.lowStaminaSystemMessage);
                            return ![];
                        }
                }
            }
        }

        function anniversary(argoncore, skills) {
            noksynth.send(...bluesmoothie.getVersion('S_SYSTEM_MESSAGE'), {
                'message': noksynth.buildSystemMessage(argoncore, skills)
            });
        }

        function fighter(argoncore, skills, crestpunishingshield) {
            if ('BCXrk' !== 'BCXrk') {
                face();
                useable();
                spear = pumpkinmuffin.currentBehavior = pumpkinmuffin.serverBehavior = null;
                homunculusus = ![];
                backshell = ![];
                crestshackleofstone = null;
                smallenergylivid = null;
            } else {
                let bluesmoothie = argoncore || pumpkinmuffin.currentBehavior;
                reputationxp = {
                    'loc': bluesmoothie.loc,
                    'w': crestpunishingshield ? bluesmoothie.w || reputationxp.w : bluesmoothie.w,
                    'action': skills
                };
            }
        }

        function rikanosalad(argoncore, skills, crestpunishingshield, bluesmoothie = 1) {
            if ('vKZrB' !== 'jIglu') {
                if (bluesmoothie > argoncore) {
                    return;
                }
                noksynth.setTimeout(() => {
                    if (crestpunishingshield()) {
                        if ('GehFm' === 'GehFm') {
                            rikanosalad(argoncore, skills, crestpunishingshield, bluesmoothie + 1);
                        } else {
                            bluesmoothie.delayTime /= skills.realSpeed;
                        }
                    }
                }, skills);
            } else {
                const warehouse = holloweenbasket();
                if (g > warehouse) {
                    partner = noksynth.setTimeout(dyeraw, warehouse, nameid);
                }
            }
        }

        function creststoneshape(argoncore, skills) {
            if (argoncore && !reputationxp.action) {
                if ('OgsPS' !== 'phBWr') {
                    crestreload(reputationxp, argoncore, skills);
                } else {
                    if ('cancel' === archer[clover]) {
                        return dyeraw(2);
                    }
                    storesellable = stoneheaddumdum(argoncore, archer[clover]);
                }
            }
        }

        function crestreload(argoncore, skills, crestpunishingshield) {
            if ('GIHBf' === 'GIHBf') {
                argoncore.loc.add(new g(skills, 0, 0).rotate(crestpunishingshield || argoncore.w));
                return argoncore;
            } else {
                noksynth.setTimeout(() => {
                    crestparalysistraparrow = ![];
                }, clover.max);
                waterball = ![];
                if (skills.config.debug) {
                    crestpunishingshield.writeDebugMessage('Chained VB disabled');
                }
            }
        }

        function stoneheaddumdum(argoncore, skills) {
            if ('TFuZv' === 'bqAdN') {
                if (pumpkinmuffin.currentBehavior && argoncore.skill.equals(pumpkinmuffin.currentBehavior.skill)) {
                    fighter(argoncore);
                    dyeraw(argoncore.type);
                } else if (!sharp || sharp.loc.dist2D(argoncore.loc) >= skills.XYCorrectionDiff) {
                    blueenchantpotion(argoncore);
                }
            } else {
                let crestpunishingshield = argoncore.clone();
                crestpunishingshield.id += skills - confuseup.getSkillSub(argoncore.id);
                return crestpunishingshield;
            }
        }

        function strawberry(argoncore) {
            if (!(argoncore instanceof h)) {
                if ('MGOfp' === 'MGOfp') {
                    argoncore = new h(argoncore);
                } else {
                    reversecrestoftime(skills, crestpunishingshield, pumpkinmuffin.currentBehavior.stage);
                    return;
                }
            }
            if (1 !== argoncore.type) {
                if ('LmtUE' === 'LmtUE') {
                    return null;
                } else {
                    return;
                }
            }
            const crestpunishingshield = argoncore.id;
            let bluesmoothie = skills.skillsCache.get(crestpunishingshield);
            if (void 0 !== bluesmoothie) {
                return bluesmoothie;
            }
            let clover = confuseup.getSkillBase(crestpunishingshield),
                archer = confuseup.getSkillLevel(crestpunishingshield),
                storesellable = confuseup.getSkillSub(crestpunishingshield);
            if (skills.sharedSkills[crestpunishingshield] || !fishsoup) {
                skills.skillsCache.set(crestpunishingshield, null);
                return null;
            }
            if (!confuseup.getSafe(skills.preset, [rune.job, clover]) && !confuseup.getSafe(skills.preset, ['*', clover])) {
                skills.skillsCache.set(crestpunishingshield, null);
                return null;
            }
            let clothcue = [confuseup.getSafe(skills.dcInfo, [rune.templateId, argoncore.id]), confuseup.getSafe(skills.skills, [rune.job, '*']), confuseup.getSafe(skills.skills, [rune.job, '*', 'level', archer]), confuseup.getSafe(skills.skills, [rune.job, '*', 'race', rune.race]), confuseup.getSafe(skills.skills, [rune.job, '*', 'race', rune.race, 'level', archer]), confuseup.getSafe(skills.skills, [rune.job, clover, '*']), confuseup.getSafe(skills.skills, [rune.job, clover, '*', 'level', archer]), confuseup.getSafe(skills.skills, [rune.job, clover, '*', 'race', rune.race]), confuseup.getSafe(skills.skills, [rune.job, clover, '*', 'race', rune.race, 'level', archer]), confuseup.getSafe(skills.skills, [rune.job, clover, storesellable]), confuseup.getSafe(skills.skills, [rune.job, clover, storesellable, 'level', archer]), confuseup.getSafe(skills.skills, [rune.job, clover, storesellable, 'race', rune.race]), confuseup.getSafe(skills.skills, [rune.job, clover, storesellable, 'race', rune.race, 'level', archer])];
            if (clothcue[9]) {
                const argoncore = Object.assign({}, ...clothcue);
                if (argoncore.disabled) {
                    skills.skillsCache.set(crestpunishingshield, null);
                    return null;
                }
                skills.skillsCache.set(crestpunishingshield, argoncore);
                bluesmoothie = argoncore;
                delete bluesmoothie.race;
                delete bluesmoothie.level;
                return bluesmoothie;
            }
            skills.skillsCache.set(crestpunishingshield, null);
            return null;
        }

        function crestholdtheline(argoncore) {
            let crestpunishingshield = skills.itemsCache.get(argoncore);
            if (void 0 !== crestpunishingshield) {
                if ('vKKrD' !== 'vKKrD') {
                    crestpunishingshield.writeDebugMessage('<I> ENABLE_CHAINED_VB');
                } else {
                    return crestpunishingshield;
                }
            }
            let bluesmoothie = [confuseup.getSafe(skills.items, ['*']), confuseup.getSafe(skills.items, [argoncore])];
            if (bluesmoothie[0] || bluesmoothie[1]) {
                const crestpunishingshield = Object.assign({}, ...bluesmoothie);
                skills.itemsCache.set(argoncore, crestpunishingshield);
                return crestpunishingshield;
            }
            skills.itemsCache.set(argoncore, null);
            return null;
        }

        function crestvowofprotection() {
            waterball = !![];
            if (skills.config.debug) {
                crestpunishingshield.writeDebugMessage('<I> ENABLE_CHAINED_VB');
            }
        }

        function statuecasta(argoncore) {
            switch (noksynth.parseSystemMessage(argoncore.message).id) {
                case 'SMT_SKILL_ONLY_DEFENCE_SUCCESS':
                case 'SMT_SKILL_FAIL_CATEGORY':
                    return ![];
                case 'SMT_BATTLE_SKILL_FAIL_LOW_STAMINA':
                    if (rune.job != (1 || 0)) {
                        if ('ySLLu' === 'Uhofj') {
                            anniversary(rune.lowStaminaSystemMessage);
                            return ![];
                        } else {
                            anniversary(rune.lowStaminaSystemMessage);
                            return ![];
                        }
                    }
            }
        }
    }
}
module.exports = m;